# 회귀 테스트 (krjam-planning · krjam-jebo · krjam-fnc · 랜딩)

구조 변경 전후의 동작 동일성을 확인한다. **리팩터링 전에 baseline 을 찍고, 각 단계마다 다시 돌려 diff 가 없어야 한다.**

```bash
npm i -g puppeteer-core   # 또는 임시 폴더에 설치
node test/regress-krjam-planning.js
```

- 로컬 http 서버 + 실제 Chrome(headless). `/api/*` 는 **전부 목업** → 운영 KV 를 건드리지 않는다(§16.6).
- 커버: 부팅·탭 / 콘텐츠 보드(빈 슬롯·정렬·필터·드래그) / 캘린더 / 일정표(좌표 픽셀·취재 불필요 회색·토글·모달·전체기간) / 소식 제보→일정(날짜 분기) / 자료실(미리보기·업로드 모달·R2 13청크) / 대시보드 · 인원 / 콘솔 에러 0.
- 생성 id(`mkid()`)는 실행마다 달라지므로 비교 시 정규화:
  `diff <(grep -E "PASS|FAIL" base.txt | sed -E 's/#extra#[a-z0-9]+/#extra#ID/g') <(...)`

⚠️ 목업이라 **실제 로그인·서버 저장 경로는 검증하지 않는다.** 그 부분은 배포 후 수동 QA.

## krjam-jebo

```bash
node test/regress-krjam-jebo.js
```
제보 페이지 전면 재작성(v0.9.173) 후 동작 동일성을 지킨다 — POST 바디 계약 · 검증 순서 · ko/en(오류 문구 재번역) · 희망 시각 zero-pad · 사진 파이프라인 · 모바일/데스크톱 레이아웃(터치 타깃·CTA 위치·넘침).

## krjam-fnc (급식편의본부 OT 플립북)

```bash
node test/regress-krjam-fnc.js      # 92 케이스
```
정적 자산만 쓰므로 **API 목업이 필요 없다**(서버·KV 무접촉). 커버: 31쪽 이미지·썸네일·PDF(31쪽)·OG 존재 및 200 · 넘김(버튼/키보드/해시/이미지 실제 교체) · 스크러버 · 목차(좌측 사이드바/모바일 드로어·챕터 8개·썸네일 16:9·활성·이동·접기·상태 저장) · 확대 · 딥링크(`#p12`·범위 밖) · 모바일(레이아웃·터치타깃·좌측 드로어·가로 보기 전환/배율/확대잠금) · 이어보기 모드(세로 스크롤·현재 쪽 추적·모드 전환) · 콘솔 에러 0.
네 케이스는 실제로 잡은 버그를 고정한 것이다 — **숨긴 오버레이의 클릭 가로채기**(`[hidden]` vs `.toc{display:flex}`) · **썸네일 16:9**(img `height` 속성이 `aspect-ratio` 를 무력화) · **가로 보기 배율**(`.book{flex:none}` 없으면 눌림) · **목차 열림 상태가 모바일로 새는 문제**(넓은 화면에서만 저장).

## 랜딩 `/`

```bash
node test/regress-landing.js        # 38 케이스
```
연출(v0.9.239)이 **사용성을 해치지 않는지**를 지킨다 — 도구 카드 5개·링크 정확 · 진입 애니메이션이 끝난 뒤 전부 보이는가 · 배경 레이어가 클릭을 가로채지 않는가(`pointer-events:none`) · 가로 스크롤 없음 · 카드 클릭 시 상단 로딩바/카드 로딩 상태 · 모바일 1열·터치 타깃 · **모션 줄이기 설정에서 장식 애니메이션이 꺼지고도 내용은 그대로**.
v0.9.240 이펙트 강화 후 추가: 불티 개수(PC 24 / 모바일 14 / 모션줄이기 0) · 커서 기울기·광원 갱신/복귀 · **후광이 카드 밖으로 새지 않는지** · **호버 상태 본문 대비를 실제 픽셀로 측정해 4.5 이상**(스크린샷을 페이지 내 canvas 로 디코드 — 추가 의존성 없음).

## krjam-planning 모션

```bash
node test/regress-krjam-planning-motion.js   # 25 케이스
```
v0.9.243 에서 붙인 모션이 **운영을 방해하지 않는지**만 본다 — 상시 무한 애니메이션이 통신 진행 표시뿐인가(랜딩과 달리 배경 레이어를 두지 않기로 했다) · 화면 전환이 끝난 뒤 `transform` 이 남지 않는가(남으면 섹션 안 `position:fixed` 모달 기준이 뒤틀린다) · **통신 표시가 켜지고 반드시 꺼지는가**(카운터 음수 방지, 실제 저장 경로 포함) · 터치 타깃 유지 · 모션 줄이기에서 정지. `/api/*` 는 전부 목업이라 운영 KV 무접촉.

## 전 화면 접근성 스윕

```bash
node test/regress-a11y-sweep.js       # 32 케이스 (8화면 × 2해상도)
```
`/` · `/tour` · `/krjam-cardnews` · `/krjam-dcount` · `/krjam-jebo` · `/privacy` · `/krjam-planning`(게이트) · `/krjam-fnc` 를 모바일 390 · PC 1440 에서 열어 **글자 ≥13px · 조작 요소 ≥40px** 를 전수로 잰다. 규칙은 **검사가 있는 화면에서만** 지켜진다 — 이 스윕이 없던 동안 6개 화면에 139건이 쌓여 있었다(v0.9.248).
예외는 파일 상단 `EXCEPT` 에 **사유와 함께** 적는다(지도 핀 글자·OSM 출처 표기). `display:inline` 인 링크는 조작 요소로 세지 않는다.

## 보도자료 게시판 (krjam-planning 보도자료 탭)

```bash
node test/regress-krjam-press.js      # 31 케이스
```
v0.9.249 에서 아코디언을 게시판으로 바꾼 계약을 지킨다 — **본문을 목록에 끼워 넣지 않는가**(아코디언이 되살아나면 잡힌다) · 읽기 모달(본문 정화·첨부·매체·단계 강조) · 다중 선택과 일괄 단계/삭제 · **권한 없는 글을 건드리지 않는가**(관리자 3건 vs 부원 2건) · 서버 `cleanStatus` 정규화(구 `released` → `published`, 모르는 값 차단).
⚠️ `Auth.isStaff()` 는 관리자이거나 **MANAGE_TABS**(staff·contacts·orginfo·protocol) 권한이 있는 부원이다. 세션 픽스처에서 `tabs:['press']` 로는 기사 탭이 보이지 않는다.

## 기사 게시판 (krjam-planning 기사 탭)

```bash
node test/regress-krjam-news.js       # 94 케이스
```
v0.9.250 에서 홍보부원 기사 탭을 아코디언 → 게시판으로 바꾼 계약을 지킨다.
- **작성 입력 순서**를 그대로 고정한다 — `제목 → 부제목 → 내용 → 사진 → 태그 → 단계`. 사용자가 직접 정한 순서라, 필드를 하나 끼워 넣다 순서가 밀리면 여기서 잡힌다.
- **단계 3종**(`draft` 초안 · `reviewed` 최종검수 완료 · `published` 퍼블리싱 완료)과 **구 `published`(boolean) 유도** — 기존 기사는 마이그레이션 쓰기 없이 읽을 때 승격된다.
- ⚠️ **부제목·태그·단계가 서버 화이트리스트(`functions/api/jp-news.js`)에 있는지도 검사한다.** 없으면 저장은 성공한 듯 보이고 값만 조용히 사라진다(데이터 소실 패턴 ①).
- **우선순위·영문·서버 알림함(v0.9.252)** — 별점 0.5 스냅과 **왼쪽 절반=.5** 클릭 · 같은 값 재클릭 시 0 · 영문 4종 화이트리스트 · 세 필드가 **버전을 올리지 않는가** · 알림 수신자 판별 · **알림 실패가 코멘트 저장을 막지 않는가** · **서버가 죽으면 화면 유도로 되돌아가는가**.
- **사진 담당자 · 장문 코멘트 · 알림(v0.9.251)** — 담당자는 이름으로 저장하고 **버전을 올리지 않는가** · 코멘트 칸이 `textarea` 이고 **Enter 로 전송되지 않는가**(전송되면 둘째 줄을 못 쓴다) · 줄바꿈이 `pre-wrap` 으로 보이는가 · 알림 대상이 **내 기사/참여 기사의 남의 코멘트**뿐인가 · 배지·모두 읽음·항목 클릭 시 모달.
- **버전(v0.9.251)** — 생성 V1 · 수정 때만 +1(단계 변경으로는 안 오름) · 히스토리에 그 판의 내용 전체 · 코멘트에 기준 버전 각인 · v 없는 옛 코멘트는 시각으로 역산 · **PUT 이 코멘트를 건드리지 않는가** · 버린 판 수를 남기는가.
- **아코디언 시절 기능이 모달에 살아 있는가** — 검수 코멘트(등록칸 포함)·카드뉴스 만들기. 화면을 갈아엎으면 부르는 곳만 없어져 조용히 사라진다(실제로 사라졌다).
- **모바일 실측** — 보이는 th 수 == 보이는 td 수(열 어긋남) · 표 가로 넘침 0 · 제목 칸 120px 이상 · 모달 바닥 버튼이 모달 밖으로 안 나감.
- **접근성 실측** — 목록·읽기 모달·작성 모달 안의 글자 13px+ / 조작 40px+, 터치 폭에서 서식 도구·코멘트 삭제 x 가 실제로 40px 인지. 화면 단위 스윕은 모달 안을 못 본다.
- 그 외 — 본문을 목록에 끼워 넣지 않는가 · 읽기 모달(본문 정화·사진·태그·단계 강조·삭제 시 닫힘) · 태그 칩 입력(Enter 커밋·중복 차단) · 다중 선택과 일괄 단계/삭제 · **일괄 삭제는 관리자만**(부원은 호출 0건).

## 급식편의본부 안내 보드 (`/krjam-fnc`)

```bash
node test/regress-krjam-fnc-board.js   # 36 케이스
```
v0.9.253 에서 플립북을 **찾아 쓰는 화면**으로 바꾼 계약을 지킨다.
- 화면 9개가 **비지 않는가** — 내용을 문자열로 조립하므로 한 곳만 깨져도 화면이 통째로 빈다(원문 보기는 iframe 으로 확인).
- **자료 숫자 12종이 원문과 같은가**(941/157명 · 494+114명 · 299/284명 · 200/50명 · 06:00~07:00 · 30분간만 …). 잘못 옮기면 현장에서 사람이 헛걸음한다.
- ⚠️ **식사 메뉴가 서버(/api/jp-meals)에서 오는가**, 그리고 **보드 코드에 메뉴가 적혀 있지 않은가**. 적어 두면 홍보부에서 고친 메뉴와 반드시 어긋난다.
- 메뉴 API 가 죽어도 화면이 살아 있는가 · 카운트다운과 '지금 식사 운영 중' 이 실제 시각으로 계산되는가 · 체크리스트가 저장되는가 · 검색 · 원문 딥링크 · 접근성(13px·40px)과 모바일 가로 넘침.

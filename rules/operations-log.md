# 운영 로그 (OPS) — 라이브 KV/데이터/인프라 조치
> 버전 bump 없는 라이브 조치도 **모두** 기록(일시·대상·전후·검증). 새 항목은 이 파일 맨 아래에 append.

### OPS 2026-06-28 — krjam-planning 라이브 KV ‘JHG 회의실’→‘JHQ 본부’ 일괄 정정 (사용자 지시)
- 지시: "JHG 회의실로 되어있는거 모두 다 JHQ 본부로 변경." 코드 변경(§16.44)과 별개로 운영 KV도 정정.
- 전(前): `jp:events` 8건(memo 장소: JHG 회의실) + `jp:timetable` 8건(place: JHG 회의실). 합 16건.
- 조치(curl read-modify-write, 비파괴): 전체 배열 그대로 받아 ‘JHG 회의실’→‘JHQ 본부’만 치환 후 events/timetable 각각 PUT(author=system). 그 외 키 불변.
- 후(後)/검증: 라이브 `GET /api/jamboree-plan` 전수 스캔 — JHG 0건, ‘JHQ 본부’ 16건.

### OPS 2026-07-16 — krjam-planning 라이브 KV 일정표 종류 색 정정 (v0.9.171 STEP3)
- 배경: `defaultTtCats()` 기본값 수정은 **저장된 종류가 없을 때만** 적용된다. 라이브 `jp:ttcats` 에는 옛 색이 남아 **실제 보드에서 계속 안 읽히는 상태**였다.
- 전(前) 7종: 개·폐영식 #C0492F(4.96) · 프로그램 #2F5D4A(7.54) · 행사 #6B4FA0(6.46) · **홍보활동 #0F8A8A(4.18 미달)** · 회의 #2E6FAE(5.25) · 이동·기타 #7A6A57(5.22) · 일과 #504E48(8.32).
- 조치: read-modify-write(비파괴) — **미달 1종만** `홍보활동 #0F8A8A → #0B6E6E`(6.05). 통과하는 6종은 손대지 않음(§16.24 '일과' 정정과 같은 방식). 전체 배열 그대로 받아 치환 후 `PUT {ttcats, author:'system'}`.
- 후(後)/검증: 라이브 `GET /api/jamboree-plan` → **7종 전부 4.5+ 통과**, 종류 수 7 보존.

### OPS 2026-07-15 — R2 버킷 `jimmyport-assets` 생성 (v0.9.163 자료실 100MB)
- 지시: "자료실에 파일 업로드 용량을 1개당 최대 100MB로 제한해줘" → KV 25MiB 한계로 불가 → AskUserQuestion에서 **"R2 도입해서 진짜 100MB"** 선택.
- 전(前): 계정 R2 버킷 4종(banginoja-media·dreampath-attachments·gilwell-media-images·mokgo-photos) — jimmyport용 없음.
- 조치: `npx wrangler r2 bucket create jimmyport-assets` (Standard) + `wrangler.toml` `[[r2_buckets]] binding="SCOUT_R2"`. 기존 KV·데이터 불변(신규 대용량 첨부만 R2, 기존 `/api/file` KV blob은 그대로 서빙).
- 후(後)/검증: 버킷 생성 확인. 라이브 바인딩 적용은 배포 후 확인 필요 — 미적용 시 `/api/r2`가 503 `r2_unbound`로 명시적 실패(8MB 이하 업로드는 기존 KV 경로라 영향 없음).

### OPS 2026-06-25 — krjam-dcount D-Count 신청 데이터 전체 초기화 (사용자 지시)
- 지시: "dcount 기존 등록된거 모두 삭제하고 전체 신규 받을 준비해줘." 코드/버전 변경 없음, 라이브 KV(`SCOUT_KV`)만 조치.
- 전(前) 상태: 신청 1건 `dcount:app:홍길동`(D-27/2026-07-09, status 반려, 테스트 데이터) · `dcount:index` 1건 · `dcount:log` 3건(신청·승인·반려) · 활성 lock 없음 · 닫힌 슬롯 없음.
- 조치(`wrangler kv ... --remote`): ① `dcount:app:홍길동` **삭제** ② `dcount:index` → `[]` ③ `dcount:log` → 초기화 마커 1건(`{action:"기록 초기화",count:3}`, 내장 clearlog 방식·감사추적). `dcount:style`(디자인 설정)·`dcount:visits`(방문카운터) 유지.
- 후(後)/검증: KV `dcount:index=[]`·`app:홍길동` 부재 + 라이브 `GET /api/krjam-dcount` → `approved:[]`, 슬롯 **36/36 신청가능**(D-40~D-5), 점유 0. 신규 신청 수신 준비 완료.

### OPS 2026-06-25 — krjam-dcount 신청 기록(dcount:log) 완전 비움 (사용자 지시 "다시 로그 밀어줘")
- 지시: 잘못 들어간 테스트 정리 차원에서 dcount 기록을 다시 밀어달라. 코드/버전 변경 없음.
- 전(前): `dcount:index=[]`·`dcount:app:*` 없음(이미 비어 있음) · `dcount:log` = 초기화 마커 1건(`기록 초기화`). 새 등록 데이터는 없었음.
- 조치: `dcount:log` → `[]` (마커까지 제거, 완전 비움). 그 외 키 변경 없음.
- 후(後)/검증: KV `dcount:log=[]`·`dcount:index=[]` + 라이브 `GET /api/krjam-dcount` → `approved:[]`, 슬롯 36/36 신청가능.

## 2026-07-28 — 저장 보호 도입에 따른 KV 키 추가 (v0.9.277)
- 새 KV 키 2개가 **쓰기 시점에** 생긴다(사전 생성 불필요, 기존 데이터 변경 없음):
  - `jp:fnc-staff:snap` — 운영요원 명단 덮어쓰기 직전 상태 최근 10개(TTL 30일)
  - `units:snap` — 스카우팅 지도 덮어쓰기 직전 상태 최근 10개(TTL 30일)
- `fncauth:rl:<IP>` — 급식 관리자 로그인 시도 카운터(TTL 10분). 자동 만료되므로 정리 불필요.
- **운영 KV 에 대한 쓰기 검증은 하지 않았다.** 서버 동작은 배포되는 모듈을 가짜 KV(Map)로 돌려 확인했고, 라이브는 GET 만 확인한다.
- 되돌리기 사용법(사고 시): `GET /api/jp-fnc-staff?snapshots=1` 또는 `GET /api/units?snapshots=1` 로 시각 확인 → 같은 경로에 `PUT {"restore":"<at>"}`. 둘 다 관리자 인증 필요하고, 복구 직전 상태도 스냅샷으로 남는다.

## 2026-08-14 — 종료 서비스 KV 전량 로컬 백업 (읽기만 · 삭제는 연말 예정)
사용자 결정 6건: ① 서버 데이터는 **올해 말 일괄 삭제** ② **개인정보는 모두 로컬 백업** ③ 급식 명단 전화번호 원본은 불필요 ④ 참가자에게 종료 **알리지 않음** ⑤ 되살릴 가능성 없음(**저장만 확실하면 됨**) ⑥ **서버 부담 감소도 목표**.

- **조치는 읽기(`wrangler kv key get --remote`)뿐이다. 쓰기·삭제 0건.**
- 백업 위치: `~/Desktop/Legacy/서버백업-KV-20260814/` (사용자 보관 · 외장 이관 예정)
- 받은 키 **66/66**(실패 0): `dcount:*` 59 · `jamboree*` 3 · `jp:fnc*` 4 + 종료 라우트 `hit:*` 21.
- 🔴 **디데이 신청 29건 = 승인 26 + 반려 3.** 공개 API 는 승인분만 주므로 **반려 3건은 웹으로는 받을 수 없었다** — KV 직접 조회로만 잡힌다. 전화번호·IP·5종 동의기록이 29건 전부에 있다.
- 사진 대조: KV 신청 레코드가 참조하는 id **29개 = 이미 받아 둔 29장과 정확히 일치**(빠짐·여분 0, 파일 크기까지 확인). 62MB.
- 사람이 읽는 형태로 2종 생성: `디데이-신청자-전체-29건.csv`(이름·전화·소속·상태·IP) · `디데이-동의기록-29건.csv`(개인정보/만14세/초상권/제3자초상/사용권 — 사진 활용의 법적 근거).
- ⚠️ `dcount:lock:*` 26개는 **JSON 이 아니라 이름 문자열**이다(선점자 이름). `.txt` 로 저장했다 — JSON 으로 파싱하려 들면 전부 실패한다.

### 연말 삭제 절차 (아직 실행 안 함)
`scripts/purge-retired-kv.js` — 기본은 미리보기, `--confirm` 을 붙여야 실제로 지운다. 예정 삭제량 **95개**(dcount 59 · cardnews 3 · fnc 4 · 사진 29) → 367 → 272개, **−62MB**.
- ⚠️ **접두사로 지우면 운영 중인 자료가 날아간다** — 그래서 허용 목록 방식으로 만들었다:
  - `jp:` 73개는 **홍보부 운영 자료**(`jp:meals`·`jp:roster`…). `jp:fnc*` 4개만 급식본부 것.
  - `img:` 118개 중 **89개는 소식 제보·홍보부 사진**. 백업 사진 목록에 있는 디데이 29개만 지운다.
- ⚠️ **백업이 불완전하면 아무것도 지우지 않고 멈춘다** — 지울 키마다 백업 파일, 사진은 **바이트 크기까지** 대조. 이빨 확인 4종(키 파일 은닉 · 사진 은닉 · 사진 1바이트 변조 · 백업 폴더 부재) 전부 중단됨을 확인.
- `hit:/krjam-*` 21개는 `expirationTtl` 90일이라 **자동 소멸** — 지우지 않는다.
- 실행 후 이 로그에 날짜·건수·실행자를 남길 것.

### 미결(연말 삭제 시점에 같이 판단)
`/service-ended?s=dcount` 안내문이 *"신청 정보와 인증 사진은 모두 안전하게 보관하고 있습니다"* 라고 적고 있다. **KV 를 지우면 이 문장이 사실과 달라진다** → 그때 문구를 고칠지 사용자에게 확인할 것(예: "보관 기간이 끝나 파기했습니다"). 지금은 사실이므로 그대로 둔다.

## 2026-08-14 — 🔴 종료 서비스 KV **95개 일괄 삭제 (실행 완료)** · 되돌릴 수 없음
사용자 승인: *"이거 모두 파괴적 작업해도 괜찮음. 삭제해도 문제 없음"* — 연말 예정이던 작업을 **앞당겨 실행**했다.
실행: `node scripts/purge-retired-kv.js "<외장>/Legacy/서버백업-KV-20260814" --confirm`

- **전(前)** 369개 → **후(後) 274개** (-95). 지운 것: `dcount:*` 59 · `jamboree*` 3 · `jp:fnc*` 4 · **디데이 참가자 사진 `img:*` 29장(62MB)**.
- **지우지 않은 것(확인함)**: `jp:*` 73개(홍보부 운영 중) · `img:*` **89개**(소식 제보·홍보부 사진) · `units`·`visits`·`comments`·`jpn/jpm/jpt/jpa` · `hit:*`(90일 TTL 로 자동 소멸).
- **삭제 후 독립 검증**: 키 목록을 다시 읽어 ① 종료 서비스 키 **잔존 0건** ② 홍보부·투어 필수 키(`jp:meals`·`jp:roster`·`jp:marketing`·`jp:events`·`jp:timetable`·`units`·`visits`·`comments`) **전부 존재** ③ `img:` 89개 유지 확인.
- **라이브 화면 재검증**: `/` · `/tour` · `/krjam-planning` · `/krjam-jebo` · `/service-ended` · `/admin` · `/privacy` **7개 전부 정상**(5xx·JS·콘솔 에러 0, 본문 렌더됨).
- **사본은 외장에만 있다** — `/Volumes/Slave_Stick/01.Personal/02.Development/Legacy/`. 삭제 직전 재검증: 사진 29/29 바이트 일치 · KV 백업 67파일 · 개인정보 CSV 2종(신청 29건·동의 29건) · zip 무결성 정상. **이 디스크가 유일한 사본이다.**
- ⚠️ **`/service-ended?s=dcount` 문구는 그대로 뒀다**(사용자 지시). *"신청 정보와 인증 사진은 모두 안전하게 보관하고 있습니다"* — 홍보부가 실제로 보관 중이므로(서버가 아니라 외장) **여전히 사실**이다.

### 🔴 이때 겪은 사고 — 첫 시도가 0/95 로 전부 실패
- `kv key delete … --force` 를 95번 돌렸는데 **그 명령에 `--force` 인자가 없다**(`Unknown argument: force`). 다행히 **아무것도 지워지지 않았고** 스크립트가 `삭제 완료 0/95 · 실패 95` 로 정직하게 보고했다.
- 원인: 안전장치(백업 대조·허용목록·미리보기)만 이빨 확인했고 **실제 삭제 명령은 한 번도 안 돌려 봤다**. → [failures.md F-15](failures.md).
- 조치: `kv bulk delete <파일> --force` (한 번의 호출)로 교체 + **삭제 후 키 목록을 다시 읽어 대조**하는 단계를 스크립트에 넣었다. "지웠다고 믿지 않는다."

## 2026-08-14 — `stash@{0}` 삭제 (2026-07-28 부터 대기하던 항목)
- "local drift before aligning to origin/main (v0.9.274)" — 내용은 2026-06-25(v0.9.146) 시점 낡은 사본이라 적용하면 되감기가 된다. 사용자 승인 후 삭제.
- 삭제 전 `git stash show -p` 로 패치 백업(3,022줄, 스크래치패드 — **세션 종료 시 사라진다**). git 객체 `c69c3910` 은 gc 전까지 남아 있다.
- 이전에는 권한 분류기에 막혔으나 이번엔 실행됐다 → [failures.md §3](failures.md) 의 해당 항목 정정.

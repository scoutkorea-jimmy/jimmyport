# K-TransportRadar24 (`/ktransportradar24`, 옛 K-TrainRadar24 `/ktrainrader24`) — 변경 이력

> 전국 열차·항공기(·여객선·버스·지하철 순차 추가)와 강수 레이더를 한 화면에 올리는 지도.
> 열차 위치는 공표 시각표·실적 기반 추정, 항공기는 ADS-B 수신값이다. **소스는 이 저장소에 없다** — 별도 저장소
> [scoutkorea-jimmy/K-TrainRader24](https://github.com/scoutkorea-jimmy/K-TrainRader24)(비공개, 저장소 이름은 옛 이름 그대로)
> 에서 빌드해 산출물만 `ktransportradar24/` 로 복사한다. 2026-09-15(v0.9.318) 에 이름과 경로를 바꿨다.

---

## 이 서비스를 다룰 때 반드시 알아야 할 것

### 1. 여기 있는 것은 빌드 산출물이다. 여기서 고치지 마라
`ktrainrader24/` 안의 `assets/*.js`·`*.css` 는 번들이라 사람이 고칠 물건이 아니다.
고칠 것이 있으면 `K-TrainRader24` 저장소에서 고치고 `npm run build:static` 후
`web/dist/` 를 통째로 다시 복사한다. 여기서 직접 손대면 다음 복사 때 사라진다.

이 저장소의 "빌드 단계 도입 금지" 규칙과 충돌하지 않는다 — 빌드는 저쪽에서
끝나고, 이 저장소에는 완성된 정적 파일만 들어온다. 카드뉴스·디데이 React 를
해당 페이지에만 격리했던 것과 같은 방식이다.

### 2. 데이터는 미리 구워 둔 파일이다. 서버가 없다
열차 위치 계산은 전부 브라우저가 한다. 그래서 운영에 Node 서버가 필요 없고,
`api/` 아래 JSON 이 곧 데이터다.

| 파일 | 내용 | 갱신 |
|---|---|---|
| `api/index.json` | 준비된 운행일자 목록·배포 식별자 | 매 배포 |
| `api/routes.json` | 경로 형상 사전 (날짜끼리 공유) | 매 배포 |
| `api/day-YYYYMMDD.json` | 하루치 열차 목록 | 매 배포 |
| `api/rail-lines.json` | 배경 선로 형상 (OSM) | 분기 1회 |

경로 형상을 날짜마다 담지 않고 사전으로 뺀 이유는 용량이다. 30일치를 각각
담으면 같은 경부선 폴리라인을 30번 싣게 된다(약 7MB 낭비).

### 3-1. 실적(`api/history.json`)은 **매일** 갱신해야 한다
`history.json` 은 '어제·그제·3일 전'의 실제 착발 시각이다. 하루만 지나도
첫 칸이 '어제'가 아니게 된다. 주간 재빌드에 묶으면 주말에 나흘 전 기록이
표 맨 앞에 앉는다. 그래서 **일일 액션**(`.github/workflows/history.yml`)이
따로 있다.

쿼터가 다르다는 점이 이걸 가능하게 한다 — 시각표는 TAGO 공표 시각표에
기대므로 그쪽 일일 한도에 묶이지만, 실적은 한국철도공사 운행정보만 쓴다.
매일 돌려도 주간 재빌드의 한도를 갉아먹지 않는다.

손으로 돌릴 때:
```
cd ~/Desktop/VS_Code/K-TrainRader24
npm run build:history -- ../jimmyport/ktrainrader24/api
```
날짜 파일과 경로 사전은 건드리지 않는다. `index.json` 의 `historyDates` 만
고치고 `build` 는 그대로 둔다 — 그 값이 바뀌면 화면이 1MB 넘는 경로 사전을
통째로 다시 받는다.

### 3. ⏰ 약 2주 뒤에는 화면이 옛 시각표로 돈다
`api/index.json` 의 `dates` 범위를 넘기면 화면은 **가장 가까운 날**의 시각표를
띄운다(빈 화면보다 낫기 때문이다). 그 사실은 출처 배지에 `오늘치 없음` 으로
표시되지만, **눈에 잘 안 띈다.** 회귀 `regress-ktrainrader24` 가 두 가지로 잡는다.

- **'오늘 날짜가 준비돼 있다'** — 범위를 벗어났다. 재빌드·재배포 신호.
- **'오늘은 공표 시각표 기준이다'** — 날짜는 있는데 근거가 `pattern` 이다.
  TAGO 공식 시각표가 안 잡혔다는 뜻이라 시각 정확도가 떨어진다.

### 4. 이 데이터는 실시간이 아니다. 그렇게 말해서도 안 된다
어떤 API 도 **당일 지연**을 주지 않는다. 코레일 실적은 어제까지만 있고,
TAGO 는 공표 시각표(계획)일 뿐이다. 화면 하단 고지문이 그 사실을 밝힌다.
**그 문구를 지우지 마라.**

### 5. 두 API 를 쓴다. 서로의 빈 곳을 메운다
인증키는 **하나**다(공공데이터포털 계정당 하나). 활용신청이 승인된 API 는
같은 키로 전부 호출된다.

| | 잘하는 것 | 못하는 것 |
|---|---|---|
| **한국철도공사_열차운행정보** | 열차별 **중간 정차역 전부** | 계획·실적 모두 **어제까지** · 종별 없음 |
| **국토교통부_(TAGO)_열차정보** | **공표 시각표(약 2주 앞)** · 정확한 **종별** | 시·종착만 · OD 쌍으로만 조회 |

코레일에서 정차 패턴을, TAGO 에서 종별과 공식 시각을 가져와 합친다.
TAGO 는 "오늘 이 열차가 다니는가"도 알려 주므로, 안 다니는 열차가 걸러진다.

---

## 이력

### v0.9.302 — `/ktrainrader24` 신설
- 별도 저장소 `K-TrainRader24` 의 정적 산출물을 `ktrainrader24/` 로 편입.
  `pages_build_output_dir = "."` 이라 디렉터리를 놓는 것만으로 경로가 산다.
- 인증키 미설정 상태라 **시연용 표본 데이터**로 배포한다. 화면 좌상단 배지에
  그대로 표시된다. 코레일 인증키가 들어오면 실제 운행계획으로 재빌드한다.
- 회귀 `test/regress-ktrainrader24.js` **42건** 추가 — 파일 구조·목록 정합성·
  경로 형상·시각 단조성·선로 좌표 범위·실제 화면(클릭·콘솔) 을 잰다.
  puppeteer 없이 CDP 로 직접 붙어 이 저장소에 의존성을 늘리지 않는다.
- `_middleware.js` 차단 규칙과 대조 완료 — 막히는 파일 없음.
- 회귀 전체 **14개 스위트 780건 2라운드 연속 통과**(실패 0).

### v0.9.303 — 실데이터 전환 (코레일 + TAGO)
인증키가 들어와 시연용 표본에서 **실제 운행 데이터**로 바꿨다.

- 하루 **약 800~940편**, 종별은 TAGO 의 실제 값(KTX·KTX-산천·KTX-청룡·
  ITX-새마을·ITX-마음·무궁화 등). 번호로 추정하던 것을 전부 걷어냈다 —
  실제로 규칙이 없었다(01001 ITX-새마을 / 01007 ITX-마음 / 01151 무궁화호).
- 14일치를 미리 굽고, 그중 **12일이 공표 시각표 기준**이다. 나머지는
  TAGO 에 데이터가 아직 없는 날이라 실적 패턴만으로 만들었고 그 사실이
  `basis` 로 표시된다.
- 경로 형상을 **구간 단위로 중복 제거**했다. 인접 역쌍 9,942회 참조가
  형상 1,167개로 접힌다(88% 감소). 안 하면 routes.json 이 8.25MB 다.

**이 판에서 잡은 결함 넷** (전부 회귀에 고정)
- 동명이역이 통째로 사라졌다 — 양원역은 영동선(봉화)과 경의중앙선(서울)에
  하나씩 있는데 이름이 같다고 하나만 남겼다. 영동선 열차가 승부→양원
  3km 구간을 **249km** 로 달렸다. 이제 거리로 가르고, 어느 쪽인지는
  경로 문맥이 고른다.
- 정차역마다 따로 중앙값을 내니 **시각이 거꾸로 갔다**(47편). `승부 19:45
  출발 → 양원 19:42 도착`, 같은 역에서 `09:11 도착 / 09:09 출발`.
- 우회 가드 임계가 3배라 **영동선 18편을 잘못 걸렀다**. 태백→도계는 선로
  27.1km 에 직선 8.0km(비 3.4) — 통리재를 나선형으로 내려가는 실제 지형이다.
- TAGO 에 25편만 올라온 날을 '공식'으로 표시하고 950편을 '오늘 미운행'으로
  잘랐다. 데이터가 아직 안 올라온 것을 운행 중단으로 착각한 것이다.

**회귀 42 → 51건.** 순환 열차(홍성→서해선→천안→장항선→홍성)에서 시·종착
직선거리가 0 이라 비율 검사가 무너지는 것도 이번에 고쳤다.

### v0.9.304 — 등급 표시·지도 라벨·버전 표시 (화면만 교체)
`api/` 는 손대지 않았다. **번들만 바꿨다.**

- **열차 등급을 모양으로 나타낸다.** 근거는 한국철도공사 운전취급규정
  제55조 — 낮은 등급이 대피선으로 비켜 상위 등급을 먼저 보내는 실제 운영
  서열이다. 위로 갈수록 뾰족하고 길게, 아래로 갈수록 뭉툭하고 각지게 해
  색을 못 알아봐도 급이 읽힌다. 색은 종별을 나타낸다 — 같은 급행여객이라도
  ITX-새마을과 무궁화호는 타는 사람에게 다른 열차다.
  낮은 등급을 먼저 그려 겹칠 때 상위 등급이 위로 온다.
- 아이콘이 작아 안 보인다는 지적을 받아 전국 축척 기준을 0.72배에서
  1.0 으로 올리고 어두운 윤곽을 둘렀다.
- 지도 라벨에 편명·출발지→행선지·다음 정차역. 800편에 전부 달면 못 읽으므로
  축척·겹침·개수로 억제하되, 커서를 올렸거나 선택한 열차는 항상 보여 준다.
- 상단에 앱 버전과 데이터 갱신 시각을 **나눠서** 표시한다. 주간 재빌드는
  데이터만 바꾸고 코드는 그대로다 — 한 값으로 합치면 무엇이 바뀐 건지 모른다.
- 자정을 넘겨 달리는 열차가 사라지던 것을 고쳤다. ITX-새마을 01025 는
  서울 20:09 → 부산 다음날 00:57 인데, 자정에 날짜 파일이 바뀌는 순간
  아직 달리는 이 열차가 지도에서 사라졌다.

### v0.9.341 — 두 출처가 실은 같은 편을 한 다리로 (2026-09-17)

한국공항공사와 인천은 **인천을 드나드는 편을 둘 다 싣는다.** 묶기 열쇠가 `fid|공항쌍|방향` 이라 fid 가 다르면 다른 다리가 되고,
기존 짝 맞추기는 **방향이 다른 것끼리만** 하므로(출발행↔도착행) 같은 다리가 둘로 남아 있었다.

- **같은 날짜(20260916) 전·후 실측**: 겹치는 쌍 **6 → 0** · 다리 1,977 → **1,962** · 인천이 낀 다리 1,077 → 1,062.
- 합치면 **한국공항공사 다리가 등록부호를 얻는다** — 등록기호는 인천만 준다.
- ⚠️ **편명이 겹칠 때만 합친다.** 공항쌍·시각만 같고 이름이 다르면 남남이다.
  20260917 에는 그런 쌍이 여섯 있었는데 **전부 다른 편**이었다(`CA8887/OZ6710/SC4609` ↔ `QW939` · `PR403` ↔ `KE625/OZ9909`).
  공항쌍과 시각만 보고 합쳤다면 **남의 기체**(`B1693`)를 이 편 것으로 말했을 것이다.
- ⚠️ **둘 다 아는 시각이 어긋나면 합치지 않는다** — 같은 편명이 하루에 같은 구간을 두 번 난다.
- ❌ 처음에 "편명이 한 개도 겹치지 않아 짝 맞추기가 못 잡았다"고 적었던 것은 **오독이었다.** 다시 재니 겹치는 여섯 쌍이 편명을 3~5개씩 공유했다.

### v0.9.340 — 범례가 축척·확대 버튼을 덮지 않는다 (2026-09-17)

좁은 화면(390px)에서 **범례가 축척 막대·Leaflet 출처·확대 버튼 위에 얹혀 있었다.** 카드 수와 무관한 묵은 결함이다 —
v0.9.336(카드 여섯)과 v0.9.337(카드 일곱)의 그림이 **같은 자리에서 똑같이** 겹쳤다.

- 까닭은 한 줄이었다: 범례는 `bottom: 56px` **고정**인데 축척·확대 버튼은 `bottom: var(--zoom-bottom)`(좁은 화면 **118px**)에 선다.
  범례가 **그보다 아래에서 시작해 위로 자라니** 반드시 겹친다.
- 범례 바닥을 **같은 변수에 묶었다**(`calc(var(--zoom-bottom) + 12px)`). 비키는 셈을 새로 만들지 않고, 이미 두 폭에 맞춰져 있는 값을 쓴다.
- `scripts/verify-ui.ts` 의 축척 막대 겹침 검사에 **`#legend` 를 넣었다.** 여태 카드와 컨트롤만 재서 범례는 대상이 아니었다 —
  그래서 수치 검사가 다 통과하는 동안에도 그림에서는 겹쳐 있었다.
- ⚠️ **넓은 화면에서 "카드를 범례가 덮는다"고 한 것은 내 오독이었다**: 범례는 x **914~1160**, 쌓기는 x **1176~1424** 로 애초에 안 겹친다.
  맨 아래 카드가 잘려 보인 것은 가려진 게 아니라 **쌓기가 스크롤 상자**(`100vh−120−90−150 = 540px`)여서다.

### v0.9.339 — 켜고 끄는 손잡이를 한 곳으로 (2026-09-17)

지미: *"검색필터랑 범례랑 기능이 동일한 거 아니야?"* — 재 보니 **지하철·항공기에서 실제로 그랬다.**
**범례에서 체크박스를 걷어냈다.** 범례는 이제 *무엇이 무엇인지*만 말하고(견본·이름·지금 몇 개·기준 문구),
켜고 끄기는 **검색/필터 상자가 전담**한다.

- 겹친 것은 두 수단뿐이다: 지하철(도시→호선)·항공기(분류→항공사).
  열차는 필터 상자에만, 여객선·시내버스는 범례에만 있었고, 강수·항공정보도는 둘 다 없었다.
- 둘은 같은 `legendOff` 를 써서 서로 어긋나지는 않았다. 그래도 **같은 항목에 손잡이가 둘이면 어느 쪽이 참인지 알 수 없다.**
- 남길 쪽은 필터 상자다 — 거기에만 **2차 분류**(항공사·호선)와 **검색**이 있어 더 세밀하다.
- 여객선은 분류가 하나라 끄면 모드 스위치와 같은 일이고, 시내버스의 `gps`/`stop` 은 분류가 아니라 **근거**다 — 둘 다 끌 이유가 약해 잃는 것이 없다.
- ⚠️ 꺼진 항목이 범례에서 **흐리게 보이는 것은 남겼다** — 상태를 *보여주되* 조작은 한 곳에서만 한다.
- `scripts/verify-ui.ts` 가 이제 **어떤 수단이든 범례에 체크박스가 있으면 실패**시킨다.
  ✅ **검사가 실제로 잡는 것까지 증명했다**: 체크박스를 되살린 빌드에서 **exit 1** 과 `범례에 켜고 끄는 상자가 있다 — 켜고 끄기는 검색/필터 상자가 맡는다`,
  원상 복구 뒤 통과. ⚠️ 그 증명이 두 번 헛돌았는데 까닭은 검사가 아니라 **결함을 심는 쪽**이었다 —
  `node -e` 한 줄에서 `'` 로 작은따옴표를 만들려 했더니 셸을 거치며 **빈 문자로 먹혀** 앵커가 어긋났다.
  셸을 타는 한 줄 스크립트에서 따옴표는 `String.fromCharCode(39)` 로 만든다.

### v0.9.338 — 항공정보도를 화면이 직접 부른다 (2026-09-17)

v0.9.337 에서 붙였지만 **타일이 안 나왔다.** 중계가 원천에 못 닿았기 때문이다 — 그 길을 접고 화면이 V-World 를 직접 부른다.

- **막힌 곳을 재서 확정했다**: Cloudflare 엣지에서 `api.vworld.kr` 이 **8번 중 8번** 실패(502 세 번·520 다섯 번, 응답이 일정하지도 않다).
  **같은 요청이 우리 맥에서는 200 · `image/png` · 12,887 B · 0.04초**다. `api.vworld.kr` 은 Cloudflare 뒤가 아니라 한국 IP(211.188.33.95)라,
  받은 `error code: NNN` 은 원천이 아니라 **Workers 런타임이 subrequest 실패를 합성한 것**이다.
  ✅ 가설 다섯을 재서 지웠다: 배포 미반영 · 시크릿 미도달 · 응답 스트림 tee · `waitUntil` 구조 분해 · User-Agent 없음.
- **지미 결정(2026-09-17): "키를 넣고 브라우저가 직접."** `<img>` 타일이라 CORS 와 무관하고, V-World 가 공식 안내하는 사용법이기도 하다.
- ⚠️ **그래도 키를 번들에 박지 않았다.** 배포 저장소가 **공개**라 번들이 커밋되면 GitHub 코드 검색이 긁어 가고,
  키를 바꿔도 **이력에서 사라지지 않는다.** 그래서 켤 때 중계에서 받아 온다(`?config=1`) —
  방문자에게 보이는 정도는 같고, 대신 **시크릿만 갈면 되고 재빌드가 필요 없다.**
  키가 실린 응답은 **`no-store`** 로 못박았다(엣지 캐시에 남으면 시크릿을 갈아도 옛 키가 한동안 돌아다닌다).
  같은 곳에서 온 요청에만 내준다 — Referer 는 위조되니 완전한 방어가 아니라 **긁어 가는 봇을 막는 빗장**이다.
- ⚠️ `DOMAIN` 은 Referer 검증이 아니라 **요청 파라미터**다. 없으면 **정상 키라도 `INCORRECT_KEY`** 가 온다(실측).
- **중계는 지우지 않고 남겼다.** `?diag=1|2|3`(판 번호·시크릿 유무·원천 `fetch` 만 재기)이 담겨 있어,
  V-World 가 해외 접근을 열면 그대로 되살릴 수 있고 같은 자리를 다시 짚을 수도 있다. 회귀 **59건**.

### v0.9.337 — 항공정보도가 붙는다: 공역 경계 넷 (2026-09-16)

**V-World WMS 로 공역 경계 도면 넷을 띄운다** — 비행금지구역·관제권·방공식별구역·비행정보구역.
수단 카드가 한 장 늘어 **일곱**이 됐다(열차·지하철·항공기·여객선·시내버스·강수 레이더·항공정보도).
- 실측(2026-09-16 · 서울 언저리 256×256 타일): `lt_c_aisprhc` **12,887 B** · `lt_c_aisctrc` 7,135 B · `lt_c_aisadzc` 5,716 B · `lt_c_aisfirc` 8,098 B.
  빈 투명 타일이 아니라 실제 도형이 담긴 크기다.
- ⚠️ **키를 번들에 넣지 않았다.** V-World 는 인증키를 쿼리로 받고, 도메인 제한도 Referer 검증이 아니라 **`DOMAIN` 파라미터**다
  (실측: 정상 키라도 `DOMAIN` 이 없으면 `INCORRECT_KEY`, 가짜 키는 `INVALID_KEY`). 번들에 박으면 누구든 같은 문자열을 붙여 우리 키로 제 한도를 태울 수 있는데,
  **V-World 는 WMS 일일 한도를 공개하지 않아**(`OVER_REQUEST_LIMIT` 오류코드만 있다) 피해 크기를 계산할 수 없다.
  계산할 수 없는 위험은 지지 않는다 — 중계(Pages Function)가 키를 붙인다.
- ⚠️ **V-World 실패는 HTTP 200 에 XML 이다.** 상태코드로 성패를 가르면 XML 을 그림으로 착각한다.
  중계는 `content-type` 으로 가르고, 회귀 49건이 그 판정과 "응답·오류 어디에도 키가 없다"를 지킨다.
- **도면 넷을 한 요청에 합쳐 받는다**(V-World 는 요청당 레이어 4개까지). 도면마다 켜고 끄게 만들면 타일 요청이 **네 갈래로** 쪼개진다 —
  한도가 비공개라 그 대가를 잴 수 없어 스위치 하나로 넷을 함께 켠다(강수 레이더와 같은 예외).
- 켰을 때만·보이는 곳만 받는다. **도면이라 시간이 지나도 다시 받지 않는다**(강수와 다른 점이다). 엣지 캐시 1시간.
- ⚠️ **저장 금지 조항은 미해결로 남겼다.** V-World 레퍼런스에 "별도의 저장장치나 데이터베이스에 저장할 수 없습니다" 가 있고
  WMS 에도 같은 조건인지는 **약관 원문 미확인**이다(약관 페이지가 웹방화벽에 4회 모두 막혔다). 그래서 **타일을 굽지 않는다** —
  중계 캐시는 굽기가 아니라 전송 캐시이고, 그마저 1시간으로 짧게 뒀다.
- 상태 줄 문법에 **여섯 번째 근거**를 더했다: `V-World 공역 경계 도면`. 앞의 다섯은 '움직이는 것이 어디 있나'를 말하고
  이것만 '경계가 어디까지인가'를 말한다 — 그래서 수치도 낡음도 뜻이 다르다(도면에 `N대`·`N초 전 자료로 멎음` 을 붙이지 않는다).
- **아직 안 붙인 것**: WFS(`fetch` 라 CORS 로 막힌다 — 중계나 JSONP 가 필요) · 시계비행로·한강회랑·항공로·제한고도(**레이어 식별자를 못 찾았다**) · 항공장애물 OLS(미확인).
- ❌ **배포 직후 확인: 타일이 아직 안 나온다.** 화면·카드·범례·상태 줄·출처는 규칙대로 섰고 라이브 번들도 `v0.9.337` 이지만,
  **중계가 원천에 못 닿는다** — Cloudflare 엣지에서 `api.vworld.kr` 이 **502/520** 을 주고 응답이 일정하지도 않다.
  **같은 요청이 우리 맥에서는 200 · image/png · 12,887 B · 0.04초**다. `api.vworld.kr` 은 Cloudflare 뒤가 아니라 한국 IP(211.188.33.95)라,
  받은 `error code: NNN` 은 원천이 아니라 **Workers 런타임이 subrequest 실패를 합성한 것**이다.
  ✅ 다섯 가설을 재서 지웠다: 배포 미반영(`?diag=1` → 판 번호가 갱신됨) · 시크릿 미도달(`key=있음`) · 응답 스트림 tee(`arrayBuffer` 로 바꿔도 그대로) ·
  `waitUntil` 구조 분해(고쳐도 그대로) · User-Agent 없음(`?diag=2` 헤더 없음 ↔ `?diag=3` 밝힘 — **결과 같음**).
  🔎 중계에 **`?diag=1|2|3`** 을 남겨 뒀다 — 판 번호·시크릿 유무·원천 `fetch` 만 재는 길이다. 다음에 같은 자리를 다시 짚을 수 있다.
  🙋 남은 길은 **브라우저가 V-World 를 직접 부르는 것**(`<img>` 타일이라 CORS 무관 · V-World 가 공식 안내하는 사용법)인데 **키가 번들에 노출된다** —
  중계를 고른 근거가 "WMS 한도가 비공개라 피해 크기를 계산할 수 없다"였으므로, 뒤집는 것은 혼자 정할 일이 아니다.

### v0.9.336 — 인천 출도착이 실리고, 유령 다리 1,240개가 접힌다 (2026-09-16)

**인천국제공항공사 실시간 출도착을 들여왔다** — 한국공항공사 자료와 **같은 행 더미에 합쳐서**.
새 파일을 만들지 않았으므로 화면이 받는 횟수는 그대로다.
- ⚠️ **인천은 `body.items` 자체가 배열이다.** 한국공항공사는 `items.item` 안에 넣는다. 배열도 `typeof` 는 `object` 라
  조건은 통과하면서 `.item` 이 `undefined` 가 됐고, **코드 `00`·총계 1,170 을 멀쩡히 읽으면서 행만 0개**였다.
  실패한 적이 없으니 실패 로그도 없었다 — 호출 24회가 나갔고 캐시 24개도 남았는데 결과만 없었다.
- ⚠️ **코드셰어는 `masterFlightId` 에 fid 가 아니라 주편명을 담는다.** 게다가 인천은 파는 이름마다 fid 가 따로다.
  원본 fid 로 묶으면 한 기체가 이름 수만큼 갈라진다 — 실측 Slave 1,240행이 **다리 1,810개로 분열**했다.
  주편 기준 합성 키를 fid 자리에 넣어 접는다(묶기 코드는 한 줄도 안 건드렸다).
  20260916: 다리 3,217 → **1,977** · 갈라진 묶음 571 → **0**. CX426/QF8233/AA8905 가 한 다리(HKG→ICN · 기체 BHPG)로 선다.
- **등록부호가 외항사의 본선이다.** 호출부호를 IATA 편명으로 바꾸는 표가 국적사 열한 곳뿐이라 외항사는 편명 길이 막혀 있다.
  인천 자료는 등록기호를 주고 ADS-B 도 `reg` 를 주니, 편명으로 못 찾으면 등록부호로 한 번 더 찾는다.
  20260916 기준 등록부호가 붙은 다리 **1,065개**(Master 행 수와 일치).
- ⚠️ 인천은 **한쪽 시각만 안다**(출발편은 출발 시각만, 도착편은 도착 시각만). 둘 다 있어야 창을 열던 판정을
  한쪽만으로도 열게 고쳤다 — 안 고쳤으면 인천 다리가 **전부** 걸러졌다.
- 인천 조회는 **하루 500회**라 굽는 쪽에서만 부른다(하루 24회). 실패해도 빈 배열이라 한국공항공사 것만으로 계속 간다 —
  한 쪽 못 받았다고 그날을 통째로 버리면 인천 한도가 떨어진 날 **기존 기능까지 같이 죽는다**.

**아직 남은 것**: 한국공항공사 다리와 인천 다리가 같은 구간·같은 시각에 겹치는 것이 **12개 중 6개** 있다.
지우거나 합치려면 "같은 분에 같은 구간의 다른 편은 없다"를 전제해야 하는데 그게 확실하지 않다.
**틀린 기체를 붙이는 것이 중복보다 나쁘므로** 근거를 더 모아 따로 다룬다.

### v0.9.335 — 카드마다 크기를 기억하고, 외항사 이름이 한국어로 선다 (2026-09-16)

**위젯 크기 조절**(지미: "각 위젯들은 크기를 내가 키우거나 줄일 수 있게").
- 오른쪽 아래 **모서리를 잡아** 바꾼다. **쌓기 안에서는 높이만**, **끌어낸 카드는 높이·폭 둘 다**.
- **자리는 화면 비율, 크기는 픽셀**이다. 비율로 적으면 창을 넓힐 때마다 글자는 그대로인 채 상자만 커져 빈 여백이 된다.
  창이 작아지면 화면 안으로 줄이되 **기억은 지우지 않는다** — 다시 넓히면 늘린 크기로 돌아온다. 바닥 180×44.
- ⚠️ **크기는 모서리에서 시작한 누름일 때만 기억한다.** 모든 크기 변화를 저장하면 상태 줄이 길어져 카드가 커진 것까지
  방문자가 정한 크기로 굳고, 화면이 입힌 크기에도 반응해 적용→저장이 맴돈다.
- ⚠️ **쌓기 안에서 폭을 넓히면 안 된다 — 구조 때문이다.** 카드 칸이 오른쪽 가장자리 기준 248px 고정이라
  더 넓은 카드는 **오른쪽으로 자라 화면 밖으로 나가거나 잘린다**(실측에서 스위치 토글이 잘려 나갔다).
  칸 폭을 유동으로 바꾸면 상세 패널이 기대는 기준이 연쇄로 흔들린다. 폭까지 바꾸려면 카드를 끌어내면 된다.

**항공사 이름을 ICAO 로 바로 찾는다** — 인천공항 취항 항공사 명부(109종)를 실측해 실었다.
- `ANA` → **전일본공수**, `CES` → **중국동방항공**, `CPA` → **캐세이퍼시픽항공**. 지도의 호출부호가 ICAO 3자라 이 표가 본선이다.
- 명부에 없으면 국적사 표를 거쳐 IATA 로 한 번 더 찾고, **그래도 모르면 코드를 그대로** 보인다 — 이름을 지어내지 않는다.
- 두 자료가 어긋난 **8건은 취항 명부를 정본**으로(상해항공→상하이항공 · 하문항공→샤먼항공 · 칭다오항공→청도항공 …).
- ⚠️ 코드 주석의 회사 이름이 틀려 있었다: **PTA 는 파라타항공**(에어프레미아가 아니다 — 그쪽은 `YP`/`APZ`), **XUM 은 섬에어**.
  다만 명부와 대조하니 **매핑 자체는 열 줄 모두 맞았다** — 국내선 편명 매칭이 틀린 것은 아니었다.

⚠️ **이번에도 수치는 다 맞았는데 그림에서 잘려 있었다.** 손잡이 계산값·저장값·새로고침 뒤 적용이 전부 맞고
검사 다섯이 통과했는데 화면에서는 쓸 수 없는 상태였다. **그림을 보고서야** 찾았다.

### v0.9.334 — 검색 필터가 켜 둔 수단을 함께 거른다 (2026-09-16)
지미 요청: "검색필터에는 열차뿐만 아니라 활성화되어있는 것들을 필터걸게" · "지하철은 1차 도시 / 2차 호선" · "항공기는 1차 국내/국외, 2차 항공사".

- **필터 상자가 두 칸**이 됐다: 열차 종별(기존)과 **수단 칸**. `KindFilter` 가 자기 칸을 `innerHTML` 로 통째로 덮어쓰기 때문에 한 칸을 나눠 쓰면 서로 지운다.
- **켜 둔 수단만** 줄이 선다. 1차 묶음 → 2차 항목:
  - **지하철**: 지역(수도권·부산·대구·대전·광주) → 노선 33개. 노선 공식 색 견본이 붙는다.
  - **항공기**: 운항 구분(국내·국제·통과·미상) → 항공사. 항공사는 **호출부호 앞 세 글자**로 가른다(2026-09-16 라이브 89대 실측: 코드 꼴 97%).
  - **여객선·시내버스**: 없다 — 여객선은 한 분류라 거를 것이 없고, 시내버스는 고르기 줄이 이미 노선을 고른다.
- ⚠️ **1차는 분류 축이 아니라 묶음**이다. 지하철의 분류 축은 노선 하나이고(classification.md), 지역은 33줄을 접기 위한 것뿐이다. 열차가 이미 같은 선례다(분류는 종별, 필터는 등급으로 묶음).
- ⚠️ **항공기 1차는 네 분류 그대로** 둔다. '국외'로 뭉치지 않는다 — 통과는 국외가 맞지만 **미상은 국외인지 모르는 것**이라 거짓이 된다.
- **상태는 범례와 한 곳(`legendOff`)을 쓴다.** 저장소를 따로 두면 같은 것을 두고 범례와 필터가 다른 말을 한다.
- **항공사 이름표 45종**을 이미 받아 둔 한국공항공사 응답에서 뽑아 구웠다(호출 0회 · 코드당 이름 충돌 0). 이름을 모르는 외항사는 **코드를 그대로** 보인다 — 지어내지 않는다.

⚠️ 배치에서 두 번 헛돌았다: 스크롤 상자를 `#filter` 로 착각해 좌표를 재는 바람에 **"보인다"는 수치를 받고도 그림에는 안 나왔다.**
진짜 스크롤 상자는 `#tools`(44vh)이고 그 안에 열차 줄(27vh)·수단 줄(27vh)이 각각 스크롤한다.
그 과정에서 내가 고쳤다고 여긴 CSS 두 곳이 **더 구체적인 선택자에 덮여 무효**였던 것도 드러나 되돌리고 까닭을 주석에 남겼다.
이번엔 **수치와 그림을 둘 다** 보고 확인했다(그룹 5 · 항목 33 · 자체 스크롤 · 뷰포트 안).

### v0.9.333 — 범례를 옮긴 방문자의 화면이 죽던 것을 고친다 (2026-09-16)
**라이브 장애 수정.** 지미가 "새로고침하니까 지도가 안 보인다" 고 알렸고, 실제로 **부팅이 `선로망을 불러오는 중…` 에서 멎어 있었다**.
콘솔은 `Uncaught ReferenceError: Cannot access 'Zi' before initialization` — 네트워크도 캐시도 아닌 **코드 결함**이다.

- **원인**: `placeLegend()` 가 `cardsWide()` 를 부르는데, `cardsWide` 가 **`const` 화살표 함수**로 한참 아래에 선언돼 있었다.
  `const` 는 호이스팅되지 않으므로 모듈이 위에서 아래로 실행되는 **도중에** `setModeLegend → renderLegend → placeLegend` 가 불리면
  아직 선언 전인 값을 만져 죽고, **부팅이 통째로 멎는다**. `cardsWide`·`viewSize` 를 **`function` 선언**으로 바꿨다.
- ⚠️ **범례를 끌어 옮긴 사람만 죽었다.** `placeLegend` 는 `if (!legendAt || !cardsWide())` 라 **저장된 위치가 없으면 `cardsWide()` 에 닿기도 전에 빠져나간다**.
  그래서 재현 조건이 둘이다 — ① `ktr24.legend-at.v1` 에 옮긴 위치가 있고 ② 켜 둔 수단이 있어 범례에 줄이 선다.
- ⚠️ **화면 검사가 이 결함을 두 번 통과시켰다.** 헤드리스가 매번 **새 프로필**로 돌아 저장된 범례 위치가 없기 때문이다.
  `npm run test:ui` 만으로 "고쳤다" 고 말할 수 없다 — 평소에 안 타는 길은 **일부러 태워야** 살아 있는지 안다.
- **증명(둘 다 실측)**: 고치기 전 라이브에 상태를 심고 새로 고치니 `bootGone:false` · `running:"—"` · 같은 콘솔 오류로 **재현**됐고,
  고친 번들에 **같은 조건**을 주니 `bootGone:true` · `running:205` · **콘솔 오류 0** 이었다.

함께 나가는 것(그동안 쌓인 커밋 여섯):
- **리터럴 34곳을 디자인 토큰으로**(지미 "1번은 토큰으로 옮겨줘"): 색 12종은 이름을 주고 `var()` 로, 글자 6단계·모서리 4단계는 DESIGN.md 눈금에 정식 단계로. **값은 한 픽셀도 안 바뀐다.**
- **검색/필터 상자 규칙을 정본에 세움**: 켜 둔 수단 전부·1차 묶음 → 2차 항목(항공기 운항구분→항공사, 지하철 지역→노선, 여객선 없음).
- **항공사 이름표 45종**을 받아 둔 자료에서 구움(호출 0회, 충돌 0종) + **호출부호로 항공사를 가르는 묶음**(라이브 89대에서 코드 꼴 97%).
- 지하철 노선에 **지역**을 실어 보냄(필터 1차 묶음용), V-World 키 설명 갱신, 에이전트 작업본을 깃에서 제외.

### v0.9.332 — 지하철을 켤 때 지금 달리는 것만 받는다 (2026-09-16)
v0.9.326 에서 지하철 자료를 6.5배 줄이며 남겨 둔 숙제다. 그때도 서울 도심을 켜면 **gzip 110,562 B** 를 받았는데,
파일에는 첫차부터 막차까지 다 들어 있고 **보는 사람에게 필요한 것은 지금 달리는 운행뿐**이다.

- **먼저 쟀다**(배포본 실측, 수도권 평일 4구역·운행 9,804건): `runs` 61,081 B(55%) · 선로 형상 28,284 · 역 10,321 · 경로 7,039.
  운행을 **60분 창**으로 쪼개면 가장 무거운 창(8시대)이 13,302 B, 뼈대가 46,914 B.
- **결과**: 서울 도심 **110,562 → 60,461 B (46% 감소)**. 굽기 실측이 사후 계산(60,216 B)과 245 B 차이로 맞았다.
  구운 것은 **호출 0회 · 캐시 적중 5,066/5,066 · 9초**.
- **나누는 방식**: 구역 파일을 **뼈대**(역·구간·경로 — 하루 내내 안 바뀐다)와 **창**(운행)으로 나눈다.
  화면은 뼈대를 켤 때 한 번 받아 기억하고, 창은 **지금 창**만 받는다. 창 경계 **15분 전에 다음 창을 미리** 받는다 —
  받는 것을 확인하는 주기가 10분이라 미리 받지 않으면 경계에서 최대 10분 동안 열차가 사라진 것처럼 보인다.
- ⚠️ **뼈대가 바닥이다.** 46,914 B 는 창을 아무리 좁혀도 안 줄어든다(그 절반이 선로 형상인데 구간당 점이 중앙 5개라 더 깎을 살이 없다).
  그래서 30분으로 더 쪼개도 이득은 작고 받는 횟수만 는다 — 30·60·90·120분을 재어 **60분이 가장 나았다**.
- ⚠️ **공짜가 아니다.** 운행은 걸치는 창마다 실려 사본이 는다(9,804 → 18,300건, 1.87배). 구워 두는 양은 2.1배가 된다.
  **방문자가 받는 양이 줄고 서버에 두는 양이 느는 맞바꿈**이다.
- ⚠️ **구역 파일을 없애지 않고 나란히 굽는다.** 없애려 했더니 목록 검증(`a.file === areaFileName`)과 화면의 파일 이름 규칙이
  연쇄로 깨졌다. 나란히 두면 창이 없는 배포에서도 화면이 옛 길로 물러난다.
- **잘 도는 코드를 수술하지 않았다**: 굽는 쪽은 `serializeArea` 뒤에 나누는 단계를 붙였고, 읽는 쪽은 `readSubwayArea` 앞에 합치는 단계를 뒀다.
  그래서 검증·펴기·id 되살리기가 한 줄도 안 바뀐다.
- ⚠️ 합칠 때 두 가지를 반드시 한다: ① **중복 제거**(한 운행이 두 창에 실린다 — 그대로 이으면 같은 열차를 두 번 그린다)
  ② **곁값 자리 재매핑**(`p`·`m`·`x`·`n` 은 운행 자리를 가리켜, 중복을 지우면 자리가 밀려 **엉뚱한 운행에 급행 통과역이 붙는다**).
- **증명**: 굽기 실물 세 구역(운행 6,541건)에서 나누고 합친 것이 원본과 **완전히 일치**했다(빠짐 0 · 군더더기 0 · 통과역·시각없음·시발종착·시각 어긋남 0).
  자립 시험 셋으로 남겼고, **구역 파일을 지우고 뼈대·창만 남긴 산출물**(구역 0개 · 뼈대 18 · 창 361)로 화면 검사를 통과했다 — 옛 길이 불가능한 상태에서 돌았다는 뜻이다.
- 시험 240개 통과 · 화면 검사 390·1440 통과.

### v0.9.331 — 지나온 항적이 고도를 말하고, 범례를 끌어 옮긴다 (2026-09-16)
지미 지시 둘을 넣었다.

- **고른 비행기의 지나온 길이 고도를 말한다.** 앞쪽(남은 길)은 그대로 점선이고, **뒤쪽 실선을 고도에 따라 명도 단계로** 긋는다 — 낮으면 어둡고 순항고도로 갈수록 또렷하다.
  ⚠️ **색상(hue)으로 하지 않은 까닭**: 그러면 항공기 색 토큰이 둘(국내 금색·그 밖 은색)에서 여럿으로 늘고, 고도를 **색 하나로만** 말하게 되어 `rules/design.md` 의 "색만으로 구분하지 않는다"를 어긴다.
  DESIGN.md 가 *"나머지는 모두 잉크의 명도 단계"* 라고 이미 적고 있어 그 어휘를 빌렸다. 기체 색은 그대로다.
  **경계는 실측이다**(라이브 95대): 450~41,000 ft 에 고르게 흩어져 있어 선형 단계로도 색이 제 말을 한다(10% 3,250 · 중앙 21,000 · 90% 36,000).
  ⚠️ **고도를 모르는 점은 옛 굵기 그대로** 둔다 — 0 으로 흐리면 "낮게 난다"로 잘못 읽힌다.
- **형식을 길이로 짐작하지 않는다.** 점이 둘씩이냐 셋씩이냐를 배열 길이로 가르려 했으나 **6 은 2 와 3 둘 다의 배수**라 모호하다.
  그래서 올리는 쪽이 `dims` 로 말하고, 중계가 `trailDim` 으로 저장하고, 응답이 `dim` 으로 화면에 전한다. 읽는 쪽은 어느 쪽을 받든 **늘 셋씩**으로 맞춰 돌려준다.
- **무중단으로 넘어간다.** 수집기는 맥미니에서 돌아 다시 깔아야 새 코드가 도는데, 중계가 셋씩만 받게 했으면 그 사이 **항적이 통째로 비었을 것이다**. 둘씩도 계속 받으므로 재설치 전에도 선은 그려지고, 그때는 고도만 모른다.
- **범례를 끌어 옮기고 접을 수 있다.** 지하철을 켜면 노선이 **33줄** 펼쳐져 화면을 세로로 덮었다(지미 지적).
  끌기는 **카드가 쓰던 장치를 그대로** 쓴다 — 새로 만들면 화면에 문법이 둘 생긴다. 자리는 카드처럼 **화면 비율**로 기억하고(`ktr24.legend-at.v1`) 창이 바뀌면 다시 놓는다(픽셀로 적으면 창을 줄인 순간 화면 밖에 남는다).
- **검색 상자 제목이 `열차·역 검색 / 필터` 로 남아 있던 것**을 `검색 / 필터` 로 고쳤다(안쪽 라벨만 네 수단으로 바꾸고 제목을 놔둔 누락, 지미 스크린샷으로 발견).
- `<meta description>` 이 세 수단만 말하던 것을 **여섯 수단과 각각의 근거**로 고쳤다 — 근거가 다 다른 것들을 한 덩어리로 뭉뚱그리면 문 앞에서부터 이 화면의 원칙을 어긴다.
- 시험 237개 통과 · 화면 검사 390·1440 통과(**빌드 성공을 확인한 뒤에** 돌렸다) · 중계 회귀 66개 통과.
- ⚠️ **수집기를 다시 깔아야 고도가 쌓이기 시작한다**(`collector/install.sh`). 그전까지 항적은 정상으로 그려지되 고도를 모르는 상태로 보인다.

### v0.9.330 — 화면이 말하는 버전과 실제 배포 버전을 하나로 (2026-09-16)
상단 바에 **`v0.7.0`** 이 떠 있었다(지미 지적). 버전이 두 갈래로 돌고 있었기 때문이다 —
사이트 버전(`VERSION`)은 배포마다 올라 0.9.329 까지 갔는데, **화면이 찍는 것은 앱 버전**(`package.json`)이고 그쪽은 0.7.0 에 멈춰 있었다.

- **실측**: 라이브 번들에 `<b>v0.7.0</b>` 과 `앱 0.7.0 (빌드 2026-09-16T08:54)` 이 그대로 박혀 있었다.
  화면 HTML 에는 안 보이고 **번들 안에** 박히기 때문에(빌드 때 `__APP_VERSION__` 으로 주입) 페이지 본문만 봐서는 못 찾는다.
- **고친 것**: 앱 버전을 사이트 버전과 같은 값으로 맞췄다. 이제 둘은 **함께 올라간다**.
- **다시 어긋나지 않게 릴리스에 가드 둘을 넣었다.**
  ① `package.json` 의 앱 버전이 배포할 사이트 버전과 다르면 배포를 멈춘다.
  ② **번들에 그 버전 글자가 실제로 박혀 있지 않으면 멈춘다** — 버전만 고치고 다시 굽지 않으면 화면은 여전히 옛 버전을 말한다. 이번 고장이 정확히 그 꼴이었다.
- ⚠️ 배포 산출물에 쌓여 있던 **동기화 충돌 사본 59개**(`day-20260922 3.json` · `routes 3.json` 꼴)를 v0.9.329 에서 걷어냈고, rsync 앞에 가드를 두어 다시 끼어들면 배포가 멈춘다.

### v0.9.329 — 계획만 말하던 비행기가 실제 시각과 지연을 말한다 (2026-09-16)
지금까지 항공기가 아는 시각은 TAGO 국내선 시각표 하나였고 그것은 **계획**이다 — 지연도 결항도 실제 시각도 없다.
한국공항공사 **실시간 운항정보**(2026-09-16 활용신청 승인)를 붙여 그날 실제로 어떻게 됐는지를 말하게 했다.

- **화면 문구 예**: `계획 14:45→15:55 (시각표) · 16:47 출발 · 122분 지연 · 한국공항공사 17:28 기준`
- **받는 횟수는 늘지 않았다** — 새 파일을 만들지 않고 화면이 이미 받는 `flights/{날짜}.json` 에 `status` 를 얹는다(하루치 gzip 10.6~13.5 KB).
- **국제선도 말한다.** 국내선은 시각표 매칭으로 붙지만 국제선은 그 시각표에 없어 상태가 없었다. 호출부호에서 편명을 얻어 다리를 직접 찾는다.
  ⚠️ 한 편명에 다리가 **여럿** 달릴 수 있다(코드셰어 별칭·같은 날 왕복). 공항쌍을 알면 그것으로 고르고, **모르면 후보가 하나일 때만** 붙인다 —
  여럿 중 아무거나 고르면 남의 지연을 이 기체 것으로 말하게 된다.
- **실측(2026-09-16, 3일치)**: 다리 890·913·920(그중 국제 401·414·424) · 지연 633·508·0건 · 한국공항공사 호출 **52회 · 실패 0 · 24.8초**.
- ⚠️ **자료를 읽을 때 조심할 것 둘**
  ① `fid` 가 **물리적 한 편**이고 `flightid` 는 **파는 이름**이다 — 한 `fid` 에 AF7900·GA9988·KE1402·DL7829·OZ9782 다섯 줄이 달린 예가 있다(같은 PUS→ICN 07:00). `fid` 로 묶고 편명은 별칭으로 둔다.
  ② `line`(국내/국제)은 **여정 구분**이라 김해→인천 연결편이 '국제'로 온다. 국내/국제는 **공항 코드로** 판정한다.
- ⚠️ **출발지·도착지가 안 보이는 기체를 재서 까닭을 밝혔다**(지미 지적). 비율은 **16.6%**(라이브 48/289)·**17.2%**(그날 항적 343/1,989)로 일정하다.
  군용기가 아니라 **정기편이 아닌 기체**다: 등록부호 꼴 호출부호 **58%**(C172·A169 헬기·BE20 — 공표 시각표 자체가 없다) · 외항사 편명 26% · 호출부호 없음 8% · 국적사 장거리 8%.
  **새 자료로 메워지는 것은 1~2%뿐이다** — 한국공항공사는 인천을 운영하지 않아 914개 다리 중 인천이 낀 것이 13개다. 둘 다 아는 기체는 **22/22 일치**했으니 값이 틀린 게 아니라 **없는 것**이다.
  그래서 지어내지 않고 **상세 패널이 까닭을 말한다**.
- 지도 출처와 안내창 양쪽에 `한국공항공사` 를 넣었다.
  ⚠️ `tests/sources.test.ts` 는 두 곳의 **불일치만** 잡고 **누락은 못 잡는다** — 양쪽에 다 빠져 있으면 조용히 통과한다.
- ⚠️ `shared/flight-status.ts` 의 정규식에 이스케이프가 아니라 **실제 제어 바이트**(0x00·0x1f·0x7f)가 박혀 파일이 **바이너리로 취급**됐다.
  grep·diff·코드 리뷰가 전부 눈을 감았다(typecheck·시험은 통과). 같은 뜻의 이스케이프 네 글자로 바꿨고 동작은 그대로다.
- 시험 237개 통과 · 화면 검사 390·1440 통과(**종료 코드로 확인**).

### v0.9.328 — 묻기를 걷고, 범례를 한 곳에 모으고, 지하철을 노선 색으로 (2026-09-16)
지미 지시 다섯을 한 묶음으로 넣었다.

- **묻기(묻고 답하기)를 뺐다.** 화면·CSS·배선과 `web/src/ask.ts` 를 지웠다.
  ⚠️ 지우면서 **검사가 조용히 무뎌질 뻔한 곳**을 찾았다: `#ask-open` 이 화면 검사의 **겹침 기준 목록 10곳**에 박혀 있었다.
  런처만 지웠으면 그 기준들이 빈 값이 되어 **아무것도 안 재면서 통과**한다. 기준 목록과 묻기 검사 블록, 반환값의 `running` 까지 함께 걷어냈다.
- **검색이 네 수단을 한 번에 찾는다** — 열차·지하철·항공기·여객선. **켜 둔 수단만** 뒤진다(받지도 않은 것을 찾아 주는 척하지 않는다).
  수단마다 10건·합쳐 30건까지 보이고, 상태 줄이 `열차 3 · 지하철 2 …` 처럼 수단별 건수를 말한다.
- **지하철을 노선 공식 색으로 그린다.** 자료에 **33개 노선 색이 모두 있어** 표를 새로 만들지 않았다(1호선 `#0052A4` · 2호선 `#00A84D` · 3호선 `#EF7C1C` …).
  태그·지도 표지·상세 패널·범례 네 곳에 적용했다.
  ⚠️ DESIGN.md 에는 **이 색들을 쓰지 않기로 한 근거**가 실측과 함께 있었다 — 노선 색이 **철도 종별 색과 정면으로 겹친다**
  (1호선 ≈ SRT 파랑, 2호선 ≈ 화물 초록, 3호선 ≈ 새마을 호박). 지시대로 뒤집되 **그 근거를 지우지 않고 '알고도 택했다'로 남겼다.**
  **색만으로 가르지 않는다**는 규칙은 그대로다: 태그 첫 줄의 노선 이름과 두 칸 편성 실루엣이 글자와 모양으로 말한다.
- **범례를 우측 아래 한 곳에 모았다.** 수단 카드 안에 흩어져 있던 '구분 기준' 펼침 6개를 걷어냈다.
  **줄마다 켜고 끌 수 있고, 끈 것은 지도에서만 숨는다 — 수치는 그대로 남는다.** 0 으로 지우면 '원래 없다'와 '내가 껐다'를 못 가린다.
  열차·강수에는 상자를 두지 않는다: 열차 종별은 검색/필터 상자가, 강수는 모드 스위치가 이미 맡아 **손잡이가 둘이 되면 어느 쪽이 참인지 알 수 없다**.
  자리는 **카드 쌓기 왼쪽 옆**이다 — 우측 아래는 쌓기와 `카드 제자리로` 단추가 이미 쓰고 있어 화면 검사가 실제로 겹침을 잡았다.
- **지하철 라벨은 가리키거나 고를 때만 뜬다.** 도심 한 화면에 수백 편이 서서 상시 라벨이 지도를 덮었다. 아이콘과 고른 것의 표시는 그대로다.
- 곁들여: 범례에 들여놓은 글자 크기 `12.5px`·`10px` 가 DESIGN.md 타입 램프에 없는 값이라 `12px`·`10.5px` 로 되돌렸다.
- 시험 226개 통과(묻기 시험 하나를 걷어 227→226) · 화면 검사 390·1440 통과(**종료 코드로 확인**).

### v0.9.327 — 고른 비행기의 남은 길을 대권이 아니라 '실제로 다니는 길'로 (2026-09-16)
지금까지 목적지까지 남은 길을 **대권**(지구 위 두 점 최단선)으로 그렸다. 비행기는 그렇게 날지 않는다. 키 없이 받을 수 있는 공표 항로(AIP) 자료가 없으므로 **우리가 본 것**으로 만들었다 — 맥미니 수집기가 기체마다 수신 범위를 떠날 때 한 줄씩 적어 둔 항적(`~/.ktransportradar24/tracks/`)이 원천이다.

- **뽑는 법**: 공항 쌍마다 항적을 진행률 0~1 로 펴고 마디마다 **중앙값**을 취한다(평균이 아니다 — 회항 한 편이 평균을 통째로 끌고 간다).
  목적지(f=1)에 못을 박고 **거꾸로 걸어오며** 표본이 3편 이상인 동안만 잇는다. 앞이 얇아지면 거기서 멈춘다 — 수신기가 없는 구간을 표본 한둘로 우겨 그리지 않는다.
  그래서 **경로의 끝은 언제나 목적 공항**이고, 덜 덮인 쌍은 목적지 앞 구간만 대표로 갖는다.
- **방향을 구분한다**: `GMP→CJU` 와 `CJU→GMP` 는 다른 키다. 두 방향을 뒤집어 견주면 중앙 **14.8 km**·최대 49.4 km 어긋났고, 같은 방향 안에서 표본을 반씩 갈라 견준 흔들림은 **0.0 km** 였다 — 실제로 다른 길이다.
- **표본 3편 이상만 싣는다**(2026-09-16 실측, CJU→GMP 22편을 정답으로 두고 잰 최대 편차): 2편 5.43 km · 3편 4.04 km · 5편 2.73 km · 12편 0.84 km. 2편이면 중앙값이 곧 평균이라 한 편만 틀어져도 끌려간다.
- **화면에 붙이는 규칙**: 대표 경로에서 지금 위치와 가장 가까운 점을 찾아 **그 뒤쪽만** 잇는다(지나온 길은 긋지 않는다). 가장 가까운 점조차 150 km 밖이면 붙이지 않고 **대권으로 물러난다**. 경유가 있는 편(공항 3곳 이상)은 쓰지 않는다 — 들르는 곳을 무시한 길을 '실제 항로'라고 말하게 되므로.
- **산출물**: `api/air-routes.json` **25쌍 · 14.4 KB(gzip 5.0 KB)**. 대표 경로가 대권에서 벌어진 폭은 쌍마다 중앙 **0.7~41.4 km**(가장 큰 쪽 NRT→ICN 중앙 41.4·최대 79.1 km).
- ⚠️ **비행 계획도 항로(AIP)도 아니다.** 우리가 받은 항적의 중앙값일 뿐이고 화면도 그렇게만 말한다 — `점선 = 지난 N편의 실제 항적에서 뽑은 대표 경로(YYYY-MM-DD~)`.
- ⚠️ 아직 **하루치(2026-09-16)** 자료다. 25쌍 중 18쌍이 표본 3편으로 최소선이다. 항적이 쌓이면 다시 구울수록 촘촘해진다.
- 함께 고친 것: 화면 검사(`scripts/verify-ui.ts`)의 **지하철 흉내 자료가 schema 2 인 채로 남아 죽어 있었다**. 산출물에 지하철이 있는 빌드에서는 그 길을 안 타서 아무도 몰랐다 — 지하철 파일만 지운 사본으로 재니 옛 판 exit=1, 고친 뒤 exit=0.
- 시험 227개 통과 · `validate:build` 통과 · 화면 검사 통과(**종료 코드로 확인**).

### v0.9.326 — 지하철을 켤 때 받는 것을 6.5배 줄였다 (2026-09-16)
v0.9.324 로 지하철을 붙이면서 적어 둔 숙제다: 서울 도심에서 켜면 gzip 약 764 KB 를 받았다. 지도 한 장에 얹는 선택 수단 하나가 페이지 전체보다 무거웠다.

- **먼저 쟀다**: 한 파일(`subway-capital-3-01.json`, 1,323,336 B)에서 `runs` 가 **92.6%**, 그 중 `arr`·`dep` 두 시각 배열이 **83%** 였다. 선로 형상은 2.5% 뿐이라 **형상은 건드리지 않았다**(15 m 단순화 그대로).
- **줄인 방법은 '덜 보여 주기'가 아니라 '짧게 적기'다**(schema 3):
  - 시각을 **초 단위 차**로 적고 `arr`·`dep` 를 한 줄로 합쳤다 — 값이 단조라 차는 음수가 안 나오고 대개 두세 자리다. `-1` 채움이 사라졌고, 0.01분으로 반올림하던 schema 2 보다 **더 정확하다**.
  - 같은 **노선·방향·경로**의 운행을 묶어 되풀이되던 이름을 없앴다(5.8%).
  - `id` 는 싣지 않는다 — `요일-노선-방향-첫역-첫출발초` 라서 **읽는 쪽이 똑같이 되살린다**(5.4%).
  - 역·경로·노선은 이름 대신 색인으로, 좌표는 1e-5° 정수 차로, 직선 표시는 비트 문자열로.
- **결과(2026-09-16, 같은 캐시로 다시 구움 · 호출 0회 · 캐시 적중 5,066/5,066)**
  | 보는 곳 | 원본 | gzip |
  |---|---|---|
  | 서울 도심 z12 | 4,062,266 → **1,714,381 B** | 716,127 → **약 113,000 B** |
  | 전국 z7.5 | 5,511,834 → **2,301,158 B** | 1,008,116 → **147,587 B** |
  가장 큰 파일 gzip 237 KB → **34 KB**. 전체 굽기 결과 1,851,390 → 271,074 B(gzip).
- ⚠️ **뺀 것은 없다**: 운행 24,091건이 전후 같고, 구역·노선별 운행 수가 하나도 다르지 않으며, 못 싣는 노선 10개도 그대로다. 화면 문구는 글자까지 동일하다(`tests/legend.test.ts` 그대로 통과).
- ⚠️ 읽는 쪽이 알아야 할 것 셋(코드 주석에 박아 뒀다):
  ① `runs` 는 이제 **묶음**이라 `runs.length` 가 운행 수가 아니다(`countRuns()` 를 쓸 것).
  ② 역·구간을 **색인으로** 가리키므로, 한 역만 버리면 뒤 색인이 전부 밀린다 — 사전이 깨지면 그 파일을 통째로 버린다(운행 하나가 깨지면 그 운행만 버리는 것은 그대로).
  ③ 파일이 7배 작아져 옛 분할 기준(300 KB)이 더 이상 수도권을 쪼개지 않았다 — 인천을 보는 사람이 수도권 전 노선을 받게 된다. 40 KB 로 낮춰 구역 알갱이를 지켰다.
- 시험 215개 통과 · 화면 검사 390·1440 통과(**종료 코드로 확인**) · 지하철 굽기 호출 0회.
- 남은 여지: 운행을 60분 창으로 쪼개 지금 창만 받으면 서울 도심이 gzip **약 59 KB** 까지 내려간다(30·60·90·120분을 재서 60분이 가장 나았다). 화면이 파일 둘(뼈대+창)을 받아야 해서 다음 차례로 남긴다.

### v0.9.325 — 수단마다 따로 떼는 카드 (2026-09-16)
지미: "위젯 분리 작업들은 안하니?" → **수단별 카드를 따로 떼기**.

- **한 상자에 쌓여 있던 여섯 수단이 각각 카드가 됐다**(`#mode-card-{수단}`): 머리에 끌기 손잡이·스위치·접기 단추, 몸통에 상태 줄·고르기 줄·구분 기준.
  위젯 해부(스위치 · 상태 줄 · 고르기 줄 · 구분 기준)와 id 규칙은 그대로다 — 나뉜 것은 **담는 그릇**뿐이다.
- **접기**: 카드마다 몸통만 접는다(머리는 남는다). 켜짐과 별개 축이라 **접어도 자료는 계속 받는다**.
- **옮기기(넓은 화면 ≥721px)**: 머리를 끌어 어디로든. 화면 가장자리 16px 안에서 멈추고, 옮긴 카드는 쌓기에서 빠져 남은 카드가 위로 당겨 붙는다.
  **자리는 픽셀이 아니라 화면 비율로 기억한다** — 픽셀로 적으면 넓은 화면에서 오른쪽에 둔 카드가 좁은 창에서 화면 밖에 그려져 사라진다.
- **기억**: `ktr24.cards.v1` 에 수단별 `{접힘, 자리}`. 모양이 틀리면 통째로 기본값. 좁은 화면(≤720px)은 끌기 없이 2×2 격자 그대로이고, 접기는 된다.
- **`카드 제자리로`** 한 번으로 전부 복귀. 옮긴 카드가 없으면 할 일이 없어 숨는다.
- ⚠️ 이 단추로 두 번 틀렸고 둘 다 **좌표로** 잡았다(1440px 실측):
  ① 쌓기(스크롤 상자) 맨 끝에 두었더니 시내버스 고르기 줄이 열린 화면에서 **스크롤 아래로 밀려 안 보였다** — 되돌릴 길이 사라진다.
  ② 바닥에 붙였더니(sticky) 이번엔 그 고르기 줄을 **덮었다**(단추 [1176,620,1424,660] · 고르기 줄 [1186,548,1414,672]).
  칸(`.cards-dock`)을 두어 **스크롤은 쌓기만, 단추는 그 바깥 아래**에 세워 둘 다 아니게 했다. 회귀는 이제 두 방향을 다 잰다 — 가려지지 않는가, 그리고 **무엇을 가리지 않는가**.
- ⚠️ 이 작업을 한 번 "두 폭 통과"라고 보고했는데 **틀렸다** — `verify-ui` 를 `| tail` 로 파이프해 종료 코드가 가려져 있었다. 이번에는 종료 코드로 확인했다(exit 0 · 시험 215개 · 390·1440 통과).
- 상세 패널은 **기본 배치 기준으로만** 카드와 겹치지 않는다. 방문자가 옮긴 자리는 그의 것이고 옮긴 카드가 패널 위에 선다 — 되돌리는 길은 `카드 제자리로` 하나다(rules/widgets.md 에 적었다).

### v0.9.324 — 출발은 천천히, 도착 전에는 느리게 · 지하철 · 선착장을 바다로 (2026-09-16)
지미: "비행기, 선박, 기차 모두 다 시작에는 천천히 움직였따가 제 속도로 달렸다가 도착지에서는 조금 느려지지 않니? 그런 부분들도 실제로 반영해."

- **움직임 — 사다리꼴 가감속**: 시각표로 내는 위치(열차·지하철·여객선)가 등속이 아니라 **가속 → 순항 → 감속**으로 움직인다(`web/src/interpolate.ts`).
  구간 거리 D와 공표 시간 T에서 순항속도를 풀고, 풀리지 않거나 그 차의 영업 최고속도를 넘으면 **등속으로 물러난다**.
  ⚠️ 곡선은 **착발 시각과 거리를 건드리지 않는다** — 가감속 값이 틀려도 도착 시각은 그대로다.
  값(공개 제원·업계 통상값, 우리가 잰 값이 아니다): 고속차량 0.5/0.6 m/s²·305 km/h · 전동열차 1.0/1.0·110 · 무궁화·새마을 0.35/0.5·150 · 화물 0.25/0.5·120 · **여객선 0.12/0.08·45**(접안이 출발보다 느리다).
  실측(09-16 자료): 열차 구간의 **90.4%** 가 곡선. 물러나는 곳은 몰려 있다 — **화물 25.7% · 공항철도 직통 25.0%**(시각표가 그 차의 영업 최고속도보다 빠른 순항을 요구한다. 최고속도를 올리면 비율은 오르지만 그 차는 그 속도를 못 낸다).
- **항공기는 추측항법**: 15초마다 뛰던 점이 흐른다. 마지막 수신 위치에서 **기체가 보고한 대지속도·방위로** 민다(최대 30초 — 실제 수신 간격).
  낡거나(90초) 속도·방위를 모르면 **밀지 않고 받은 자리에 세운다**. 상태 줄(`수신 사이 위치는 추측항법`)·상세 근거·항적 끝 점선이 말한다.
- **지하철(기본 꺼짐)**: 33개 노선·평일 13,123 운행. **보이는 구역만** 받아 그린다(목록의 구역 상자와 겹치는 파일만, 지도를 옮기면 더 받는다).
  분류는 **노선**이고, TAGO 지하철 시간표에 **열차번호가 없어** 태그 1줄도 노선 이름(`서울 2호선`)이다 — 없는 번호를 지어내지 않는다. 움직임은 전동열차 곡선.
  자료가 들고 있는 구멍을 화면이 그대로 말한다: `휴일 시간표 없음`(2·7호선) · `시각 없는 역 N곳 — 거리 비례 추정`(2호선 3곳·6호선·수인분당 4곳) · `지선 일부 시각표 없음`(2호선 성수·신정 지선 8역) ·
  `운행 일부 제외(시각 잇기 실패 1.6%)`(1호선 평일) · 못 싣는 노선은 구분 기준에 까닭과 함께(공항철도는 직통 통과 시각이 앞뒤 모순, 경춘선은 회기 빼고 0행 …).
  색은 **한 색**(`#f2a2dd` 승강장 자홍)이다 — 공식 노선 색 33개는 1호선 파랑이 SRT, 2호선 초록이 화물열차, 3호선 주황이 새마을과 겹쳐 한 화면에서 색이 뜻을 잃는다. 노선은 글자가 말한다.
  ⚠️ **켜면 받는 양이 크다**: 서울 도심(z12) 4개 파일 gzip 약 **764 KB**, 전국(z7.5) 8개 약 1 MB. 기본 꺼짐이고 켠 구역만 받지만, 줄이는 일은 다음 차례다.
- **선착장 좌표**: 섬 중심점에 박혀 있던 여섯 곳을 **독립 출처 둘**(OSM 선착장 점 + 위키데이터·한국어 위키백과, 1 km 안 일치)로 못 박았다 — 제주도 15.6 → 1.15 km, 구도(서산) 11.1 → 2.28 km, 노화 이목 3.3 → 1.06 km 등.
- **섬 중심점만 아는 선착장은 가장 가까운 바다로 당겨 그린다**: OSM 에 선착장 점이 없는 섬은 섬 폴리곤 중심이 좌표로 와서, 배가 섬 한가운데서 뜨고 뱃길이 뭍에서 시작했다
  (그린 선착장 신뢰도 실측: terminal 114 · **island 142** · none 79 · verified 14 · place 3). 그리기 직전에만 당기고, 확정 별칭·선착장 점은 건드리지 않는다 — 목포·여수는 좁은 항만이 격자에서 막힌 것이지 좌표가 틀린 게 아니다.
  결과: 당긴 선착장 **256곳**(최대 3.69 km) · 직선 구간 10.3% → **4.1%**(거리 16.4% → 5.4%) · 바다에서 2 km 넘는 항 26 → 11.
  ⚠️ **당긴 자리는 선착장이 아니다** — 큰 섬에서는 실제 선착장과 반대편 해안일 수 있다(백령도 3.64 km).
- ⚠️ 별칭표의 `unresolved` 는 **아무 효과가 없다**(코드 확인 `resolveByName`) — 운항을 실제로 버리는 것은 `blocked` 뿐이다. 이번 당기기는 덜 틀리게 만든 것이고, 근본은 좌표를 찾는 일이다.
- 시험 212개 통과 · 화면 검사 390·1440 통과 · 여객선 굽기 호출 39회(캐시 99%).
- ⚠️ 회귀 `test/regress-tour-csv.js` 가 25/25 를 모두 통과하고도 **끝나지 않은 채 41분** 떠 있었다(헤드리스 크롬 미종료로 보임). 죽이고 라운드를 새로 돌려 통과시켰다 — 멎어 보이면 로그 수정 시각과 `ps -o etime` 로 재고 진단할 것.

### v0.9.323 — 여객선 뱃길을 바다로 (2026-09-16)
지미: "선박 해상 경로가 뭔가 성의가 없어보인다?" — 맞는 지적이었다. 재 보니 구간의 **42.9%**(거리 48.9%)가 두 선착장을 곧게 이은 선이었고, 가장 긴 직선이 **178 km** 라 섬과 뭍을 가로질렀다.

- **해안선으로 바닷길을 찾는다**: OSM `natural=coastline` way 31,090개를 0.004°(위도 약 444 m) 격자에 찍고 먼바다에서 물을 채워 '지날 수 있는 칸' 290만 개를 얻는다.
  저장소에는 비트로 눌러 담아 506 KB(`data/build/sea-grid.bin`, `scripts/build-sea-grid.ts`). 그 위에서 A* 로 길을 찾고 격자의 계단은 끈 당기기로 편다(`server/src/ship/sea.ts`).
- **구간마다 출처를 싣는다**(`kinds`): `osm`(실제 항로선) · `sea`(뭍을 피해 찾은 바닷길) · `straight`(둘 다 없어 곧게 이음).
  **실선은 osm 뿐**이고 나머지는 점선이다. 검증이 `straight[i] === (kinds[i] !== 'osm')` 을 강제해 추정을 실측처럼 그릴 수 없다.
  태그·상세는 `바다 위 추정 뱃길` / `일부 직선 뱃길` 로 무엇을 보고 있는지 말한다.
- **결과(09-16 빌드)**: 직선 구간 42.9% → **10.3%**(거리 48.9% → 16.4%) · OSM 항로선 57.1% · 바닷길 32.6% · 크게 돌아 버린 바닷길 22쌍 · 하루 파일 211~220 KB.
- ⚠️ **바닷길은 실제 항로가 아니다** — 수심·항만 관제·조업 구역을 모른다. 그래서 점선이고, 화면이 그렇게 말한다.
- ⚠️ 남은 직선 40개(20 km 넘는 것 14개)는 길 찾기가 아니라 **선착장 좌표 문제**다. 굽기가 바다에서 2 km 넘게 떨어진 선착장 26곳을 보고한다(제주도 15.6 km — 섬 중심점, 목포 7 km, 구도_서산시 11.1 km …). 좌표 검증은 별도로 진행 중.
- 굽기 중 길이 없는 쌍에서 A* 가 바다 전체를 뒤지다 터졌던 것(`Invalid array length`)을 상자·닫힌 칸·확장 상한으로 고쳤다. 389쌍 0.75초. 실패 시 스택을 남긴다.

### v0.9.322 — 항공기 항적 · 누르면 상세 패널 · 고르면 그것만 · 시내버스 (2026-09-16)
지미 요청: "비행기는 항적 안그려줘? 그리고 클릭하면 해당 비행기만 보여지는것도 해줘야지", "열차처럼 누르면 세부 정보 떠야지",
"모든 도시는 한국어 국문 표기법에 의거해서 도시명 작성해줘".

- **항공기 항적**: 기체를 고르면 **받은 항적(실선)** 과 **목적지까지 대권(점선 — 비행 계획 아님)** 을 긋는다.
  키 없는 원천의 항적(trace) 주소도 CORS·봇 차단이 같아, 맥미니 수집기가 15초마다 이미 받는 위치를 기체별로 모은다(30초 간격·15° 꺾이면 바로·최근 90분·120점).
  1분에 한 번 목록과 함께 올리고, 중계가 **별도 KV 키**(`ktransportradar24:aircraft-trails:v1`)에 둔다 — 목록 GET 이 항적을 매번 읽지 않게, 쓰기는 하루 1,440회 더.
  화면은 고른 기체 하나만 `GET /api/ktransportradar24-aircraft?trail=<hex>`(30초 캐시)로 받는다. 비공개(LADD·PIA)·지상·범위 밖 기체의 항적은 같은 몸통의 정리된 목록 기준으로 버린다.
  한계: 수집기가 본 구간만(수신 범위에 들어오기 전 길은 없다), 항적이 180초 넘게 낡으면 주지 않는다.
- **누르면 상세 패널 — 열차와 같은 틀**: 항공기는 분류 칩(국내·국제·통과·미상)·IATA 편명·기종·등록부호·고도·속도·진행 방위·위치 수신 나이·목적지까지 거리·
  **분류 근거**(판정 순서의 몇 번째에서 정해졌는지)·들르는 공항. 여객선은 기항지·공표 출발 시각·종착 근거·운항일.
- **고르면 그것만(모든 수단 한 규칙)**: 아이콘이나 **태그**를 누르면 고르고, 다른 층까지 전부 진하기 0.3 으로 물러서며 태그는 고른 것·커서 올린 것만 선다.
  전에는 항공기 태그를 눌러도 반응이 없었다(아이콘 반경만 판정). 규칙은 K-TrainRader24 `rules/design.md` 5-1.
- **넓은 화면에서 상세 패널을 위젯 왼쪽으로**: 같은 오른쪽 모서리라 패널이 교통수단 위젯을 통째로 덮어, 고른 동안 수단을 켜고 끌 수 없었다(열차 때부터 — 이번 검사로 드러남).
  916px 미만 넓은 화면은 왼쪽 기둥(검색 상자를 잠시 덮음), 720px 이하는 그대로 아래 시트.
- **시내버스(기본 꺼짐) — 고른 노선만**: 위젯의 고르기 줄에서 도시 → 번호 찾기로 **노선 최대 3개**를 고르면 그 노선 버스만 30초마다 받는다(서울 시내버스는 TAGO 에 없다 — 화면이 말한다).
  새 중계 `functions/api/ktransportradar24-bus.js`: `GET /api/ktransportradar24-bus?city=&route=`(위치, 엣지 30초 캐시 · 코드 99 는 직전 성공본 10분을 stale 로) · `&stops=1`(경유 정류소, 7일).
  **공용 하루 한도 보호**(오퍼레이션마다 10,000회, 오류도 셈): 남은 수 1,000 밑이면 원천을 부르지 않고 까닭을 말함 · 탭이 가려지면 멈춤 · 60분 조작 없으면 멈추고 `시내버스 다시 받기` 로만 이어 받음.
  분류 = 위치 근거: `GPS 수신`(광역시·제주 등, 15~30초) / `정류소 단위`(경기 31곳 — 정류소를 지날 때만 좌표가 바뀜, 사이 위치는 모른다고 말함). 계룡(34070)은 좌표 필드가 없어 제외.
  노선 목록은 136개 도시 21,475개 노선을 구워 `api/bus/`(2026-09-15 굽기)에 둔다. 인증키는 Pages secret `DATA_GO_KR_SERVICE_KEY`(인코딩된 값 그대로) — 저장소·응답에 없다. 중계 회귀 112건.
  색 `bus-city` #8be38b(여객선 라임·화물열차와 각각 38° 떨어진 빈자리), 층 402(열차 400 위, 여객선 405 아래), 고른 버스는 경유 정류소를 곧게 이은 **점선**(도로 선형 아님).
- ⚠️ 수집기 재설치 중 `launchctl bootstrap` 이 "5: Input/output error" 로 실패해 수집기가 약 30초 내려가 있었다(2026-09-16 00:02). install.sh 가 재시도하고 실제로 도는지 확인하게 고쳤고, 재실행에서 같은 오류를 재시도로 넘기는 것을 확인했다.

### v0.9.321 — 위젯은 한 규칙으로 · 구분 기준 펼침 · 축척 50 km·대한민국 안으로 고정 (2026-09-15)
지미 요청: "위젯들은 같은 규칙과 기준으로 풀려야지. 일부는 기준이 어떻게 나뉜지 모르겠어. 룰 폴더 만들어서 디자인과 위젯들 구분 기준을 추가해놔",
"1:50km 까지만 축소되게 락 걸어줘. 그리고 한국을 벗어나지 않게".

- **K-TrainRader24 `rules/`** — `design.md`(디자인 규칙) · `widgets.md`(위젯 해부·문장 문법·새 수단 추가 절차) · `classification.md`(수단별 구분 기준). 코드와 다르면 코드가 틀린 것.
  회귀가 **화면 문구와 규칙 문서를 글자 단위로 대조**한다(`tests/legend.test.ts`) — 한쪽만 고치면 빨개진다. 쓰다가 문서 쪽 등급 표 두 줄이 틀렸던 것을 그 대조가 잡는 구조다.
- **위젯 해부 셋, 모든 수단 같다**: 스위치 · 상태 줄 · **구분 기준 펼침**(머리말 + 분류마다 견본·이름·지금 수·기준 한 줄).
  열차는 등급으로 묶은 종별(색=종별, 모양=등급), 항공기는 판정 순서가 있는 국내·국제·통과·미상(국내선 시각표 수치는 '국내' 줄로), 여객선·강수는 한 분류와 근거.
- **상태 줄 문법 하나**: `수치 · 근거 · 한계 (· 낡음)`. 근거 어휘는 셋뿐 — `시각표 기준 추정 위치` · `HH:MM KST ADS-B 수신 위치` · `레이더 관측 강수`.
  불러오기 `{대상}을 불러오는 중…`, 실패 `{대상}을 받지 못했습니다. {없음}이라는 뜻이 아닙니다.` 를 한 함수(`wording.ts`)가 만든다.
- **지도 범위**: 최소 확대 **7.5**(제주 위도에서도 축척 막대 약 87 km → `50 km`; 7.25 는 `100 km` 가 뜬다), 경계는 대한민국(백령도·마라도·독도 포함) · 탄력 1 — 밖으로 끌 수 없다.
  좁은 화면에서는 한반도가 한 화면에 다 안 들어온다(요청의 결과). test:ui 가 억지로 줄이고 멀리 끌어 본다.

### v0.9.320 — 여객선 (TAGO 국내선박운항정보, 2026-09-15)
지미 요청: 국토교통부_(TAGO)_국내선박운항정보를 "겹쳐서 서비스", "교통수단 하나 끝날 때마다 배포".

- **위젯에 여객선**(기본 꺼짐). 켜면 `api/ships-index.json` 과 오늘·어제 `ships-YYYYMMDD.json` 을 받아 1초마다 위치를 낸다.
  좁은 화면의 위젯 스위치는 넷이 되어 **2×2** 로 바꿨다(한 줄 넷은 390px 에서 '강수 레이더'가 넘친다).
- **위치는 시각표 추정이다 — AIS 실측이 아니다.** 공표 출발 시각을 같은 배끼리 이어 운항을 복원하고(도착예정시간은 TAGO 값이 가짜라 버림), 뱃길(OSM 항로선, 없으면 직선)을
  열차와 같은 보간(`positionOf`)으로 따라간다. 종착 도착은 거리 ÷ 속력 추정. 결항·기상 통제는 모른다. 상태 줄과 초점 태그가 이것을 전부 말한다.
- **태그는 같은 문법**: `여객선 천사1호` / `목포 → 장산·신안군` / `다음 안좌 06:30`(정박 중이면 `정박 안좌 · 출발 06:53`, 마지막 구간은 `도착 추정 …`).
  커서·탭하면 `시각표 기준 추정 · AIS 실측 아님 · 일부 직선 뱃길 · 종착 추정` 과 함께 **뱃길을 그리고, 직선으로 이은 구간은 점선**이다.
- 색은 등부표 라임(`#bef264`) — DESIGN.md 에 먼저 올리고 쓴다(The Claim A Hue First Rule). 층 순서 열차(400) ← 여객선(405) ← 항공기(410), 위 층 태그가 아래 층을 피한다.
- 데이터(09-15 빌드): 749개 항 전수 → 활성 348, 호출 3,727, 15일(공표 D-1~D+8 = 10일, 나머지 5일은 같은 요일 복사 `pattern`), 그린 운항 5,020 · 좌표를 믿을 수 없어 뺀 운항 2,425(33%), 하루 187~215KB.
  선착장 좌표는 검증 별칭 14 · 차단 7 + OSM 이름 매칭·속도·운임 검사. 거점(목포·통영·완도…)은 두 번째 출처가 없다(VWorld 키가 있으면 대조 가능).
- ⚠️ 09-24~26 추석 연휴는 평일 운항을 복사한 값이다 — 실제와 다를 수 있다(배지·태그가 `같은 요일 복사` 로 말한다).
- 재빌드 순서: `build:static` → `build-flights.ts` → `build:ships` (앞이 대상 폴더를 갈아 끼우므로).

### v0.9.319 — 항공기를 열차와 같은 태그로 · 국내/국제/통과/미상 · 목적지·기종 · 축척 막대 (앱 v0.7.0, 2026-09-15)
지미 요청: "태그 방식은 동일해야지", "국내선/국제선 구분", "호버하면 목적지와 기종", "전체 톤앤매너 규칙을 하나 만들어 준용", "OSM 지도 축척이 보이게".

- **DESIGN.md**(K-TrainRader24 저장소 루트) — '야간 관제실'. 모든 교통수단이 같은 3줄 지도 태그(식별자·구간·보조), 색은 움직이는 것과 경보에만, 색만으로 구분하지 않는다.
- **항공기 캔버스 층**: Leaflet 마커·툴팁을 걷고 열차 라벨 함수와 노출 축척(9.5)·상한·자리 찾기를 그대로 쓴다. 항공기 태그가 열차(아이콘·라벨)를 피하고,
  열차는 항공기 **아이콘만** 피한다 — 서로의 태그를 피하면 한 프레임 늦게 반응하며 번갈아 뛴다. 커서·탭하면 속도·국내선 계획 시각이 한 줄 더.
- **구분**: 국내선 시각표와 맞으면 국내 → 경로 공항 국가(전부 KR 국내 · 섞임 국제 · **KR 없음 통과** — 서해 위 선양→상하이를 '국제'라 하면 거짓) → 국내선에 없는 국적사 편명은 국제 → 미상.
  국내만 금색, 나머지 은색. 태그 첫 줄 글자가 구분한다.
- **목적지**: 맥미니 수집기가 adsb.im routeset(키 없음)으로 경로를 받아 붙인다 — 새 호출부호만 1분에 한 번, 3시간 기억, `plausible` 아니면 버림. 첫 설치에서 63대 중 47대 경로.
  **기종 이름**: adsb.fi 를 먼저 받는다(`desc`, adsb.lol 은 코드만). 함수가 경로 모양을 한 번 더 검증한다(회귀 44건).
- **국내선 시각표** `api/flights/`(TAGO 국내항공운항정보, 15일·8,030편·694KB): 계획 -60~+90분 · 항로선 50km 안에서만 편명을 붙인다. 상태 줄에 '시각표상 비행 중 N편 · 편명 맞춘 기체 M대'.
- **축척 막대**: 왼쪽 아래, 미터법, 계기판 톤. test:ui 가 고지·위젯·런처·확대 버튼과의 겹침을 두 폭에서 잰다.
- 검사 결함 하나를 잡았다: verify-ui 는 브라우저 코드를 템플릿 문자열로 넘겨 **정규식의 `\d`·`\s` 가 사라진다** — '10 km' 를 못 읽었다. 역슬래시 없는 정규식만 쓴다.

### v0.9.318 — K-TransportRadar24 로 이름 변경 · 교통수단 위젯 · 항공기는 키 없이 (앱 v0.7.0, 2026-09-15)
지미 요청: "강수레이더와 항공기 정보를 별도의 위젯으로", "교통수단 단위로 켜고 끄고", "항공기는 무료 API 만, 키가 필요한 API 는 쓰지 말 것",
"K-TransportRadar 이런 식으로 이름을 바꾸고 라우팅 사이트 명도".

- **경로** `/ktrainrader24` → **`/ktransportradar24`**. 폴더를 옮기고(`git mv`) `_redirects` 에 301 두 줄 — 공유된 `?train=` 링크를 살린다.
  옛 폴더 이름에 있던 'rader' 오타는 새 경로에서 바로잡았다. 이 문서 폴더도 `docs/ktransportradar24/` 로 옮겼다.
- **위젯**: 검색/필터 상자 안 체크박스 두 줄이던 겹쳐 보기를 꺼내 열차·항공기·강수 레이더를 **수단 단위로** 켜고 끈다(`ktr24.modes.v1` 기억).
  종류 필터(KTX·무궁화…)는 상자에 남는다. 열차를 끄면 레이어를 떼고 **마지막 프레임의 클릭 좌표를 비운다** — 안 비우면 보이지 않는 열차가 클릭에 걸린다.
  상단 '운행 추정'은 `—` 로 둔다(0 은 '운행 없음'으로 읽힌다).
- **항공기 — 키 없는 원천만.** OpenSky 등록 경로(v0.9.316)를 지웠다. 엣지에서는 adsb.lol 429·adsb.fi 403 이고 둘 다 CORS 가 없어 브라우저도 못 부른다.
  그래서 **맥미니 수집기**(K-TrainRader24 `collector/aircraft.ts`, launchd `com.jimmy-os.ktransportradar24-aircraft`)가 가정 회선으로 15초마다 받아
  `POST /api/ktransportradar24-aircraft` 에 수집 토큰(Pages secret `KTR24_AIRCRAFT_INGEST_TOKEN`)으로 올리고, 함수가 LADD·PIA 를 **받을 때** 버린 뒤
  `SCOUT_KV` 키 `ktransportradar24:aircraft:v1`(10분 만료)에 둔다. GET 은 저장본이 90초 안이면 그대로, 아니면 원천 직접 시도 → 실패면 `errors` 에 `collector: 마지막 수신 N초 전`.
  옛 주소 `/api/ktrainrader24-aircraft` 는 GET 만 넘기는 별칭(한 달 뒤 삭제).
- 회귀: `regress-ktrainrader24*` → `regress-ktransportradar24*`. 항공기 중계 20 → **37건**(수집 토큰·저장본 신선도·OpenSky 제거·옛 주소 별칭).
  K-TrainRader24 `test:ui` 가 위젯 겹침(좌표)·누름 영역 40px·끈 열차의 클릭 좌표를 잰다 — 390px 에서 위젯이 접힌 검색 상자와 **4px 겹친 것**을 배포 전에 잡았다.

### v0.9.317 — 주간 재빌드 (데이터만, 2026-09-15)
코드는 앱 v0.6.0 그대로다. 데이터만 새로 구웠다.

- 범위 **20260914~20260928**(15일, 코레일 실데이터 15/15). 공표 시각표 기준 9/15일, 나머지는 실적 패턴.
  TAGO 는 여전히 약 9일 앞까지만 올라온다. 다음 재빌드 기한 **2026-09-28**.
- 최근 실적 **09-12·13·14**(1,076편). 전날까지 09-11~13 이라 하루만 더 지나면 회귀의 '끊김 표시 없음'이 빨개질 참이었다.
- 빌드 중 TAGO 가 한 번 막혔다(표본도 429) → 60초 쉬고 표본 정상 → 동시 1·간격 1초로 낮춰 재개. 재시도 예산 규칙대로 한 번 돌았다.
- 검증: `validate:build`(운영 조건) · 커밋된 `test:ui` 390/1440 · 회귀 전체 1라운드 통과 후 배포.

### v0.9.315~316 — 항공기 중계: 엣지에서 무료 집계기가 전부 막힌다 → OpenSky 경로 (함수만 교체)
v0.9.314 로 실패 이유를 담았는데 **5xx 본문을 커스텀 도메인에서 Cloudflare 가 자기 오류 페이지로 바꿔** 못 읽었다
→ 실패도 200 `ok:false` 로 준다(v0.9.315). 그제야 보인 이유:
- **adsb.lol: HTTP 429** — Workers 공유 IP 가 이미 한도를 넘겼다. **adsb.fi: HTTP 403** — 봇 차단. 둘 다 엣지에서는 못 쓴다.
  내 맥에서는 둘 다 200 이라 로컬로는 절대 알 수 없었다.
- 세 번째 원천 **OpenSky**(v0.9.316): 자격증명(`OPENSKY_CLIENT_ID`·`OPENSKY_CLIENT_SECRET`, Pages 환경변수)이 있을 때만.
  등록 사용자 4,000 크레딧/일, 한반도 상자 호출당 2 → **60초 캐시**로 하루 2,880. 익명(400/일)은 세 시간이면 바닥나 안 쓴다.
  OpenSky 는 열 배열(m·m/s)이라 readsb 모양(ft·kt)으로 바꾼다. 기종·등록번호는 없다 → 화면이 '기종 미상'.
- **⚠️ 지금 라이브의 항공기 오버레이는 "받지 못했습니다"를 정직하게 말하는 상태다.** 살리려면 지미가
  opensky-network.org 계정 → API client 를 만들고 `npx wrangler pages secret put OPENSKY_CLIENT_ID --project-name jimmyport`
  (SECRET 도) 를 넣으면 다음 요청부터 산다. 응답의 `errors` 마지막 줄이 이걸 그대로 말한다.
- 회귀 19건(OpenSky 단위 환산·지상 제외·나이 계산).

### v0.9.314 — 항공기 중계 원천 이중화 (함수만 교체)
v0.9.313 배포 직후 `/api/ktrainrader24-aircraft` 가 **502** 였다. 함수는 돌았고(pages.dev 에 내 JSON 이 그대로),
**Cloudflare 엣지에서 adsb.lol 로 나가는 요청이 실패**한 것이다 — 내 맥에서는 200 이었다. 로컬 통과가 엣지 통과를
보증하지 않는다.
- 원천을 둘로: adsb.lol → 실패하면 **adsb.fi**(`opendata.adsb.fi`, 같은 readsb 형식, 목록 키만 `aircraft`).
- 둘 다 실패하면 502 에 `errors:["adsb.lol: …","adsb.fi: …"]` 로 **어느 원천이 왜** 실패했는지 담는다. 성공 응답엔 `source`.
- 회귀 12건(adsb.fi 형식 추가).

### v0.9.313 — 강수 레이더·항공기 오버레이, 공표 시각표 재빌드 (앱 v0.6.0)

**화면에 더한 것 — 둘 다 켰을 때만 받는다(기본 꺼짐)**
- **강수 레이더.** RainViewer 공개 타일. 무료 API 는 관측값만 준다(과거 13장·예보 0장
  실측) → 상태 줄에 `HH:MM KST 관측 강수 · 예보 아님`을 못박았다. 배율 8 이상은
  어느 좌표든 같은 대체 이미지만 와서 7 을 늘려 그린다. 배경 반전 필터가 먹지
  않게 칸을 따로 둔다. 못 받으면 "비가 안 온다는 뜻이 아닙니다"라고 말한다.
- **항공기.** adsb.lol ADS-B 수신값을 이 저장소의 `functions/api/ktrainrader24-aircraft.js`
  가 중계한다. adsb.lol 은 CORS 헤더를 안 줘서 브라우저가 직접 못 부른다.
  - 엣지 캐시 **15초** — 방문자 수와 무관하게 원본 호출은 콜로당 15초 1회.
  - **LADD·PIA(비공개 요청) 기체는 중계에서 버린다.** 화면에서 숨기면 응답엔 남는다.
  - 지상 기체·위치 60초 초과 기체 제외, 지도 상자(32~39.6N·123.5~132E) 밖 제외.
  - adsb.lol 은 흔한 UA 를 `User-Agent too generic` 으로 거절한다 → 연락처 URL 을 담았다.
  - 열차(추정)와 성격이 반대(수신 실측)라 상태 줄에 `ADS-B 수신값 · 수신 범위 밖·군용기는 빠집니다`.
  - 편명·등록번호는 바깥 문자열이라 툴팁에 넣기 전에 이스케이프한다.
- 회귀: `test/regress-ktrainrader24-aircraft.js` **11건**(비공개 기체가 응답 어디에도 없는지까지).

**공표 시각표 재빌드에서 잡은 결함 — 전부 저장소 회귀에 고정**
- **429 를 일일 한도로 읽고 멈췄다.** 1,086회째 순간 차단이었고 몇 분 뒤 같은 키가 정상이었다.
  재시도 규칙을 새로 짰다(지미 지시): 빌드 전체 **재시도 5회** → 소진하면 표본 1회로
  원인 진단 → 조치(동시 1·간격 1초 / 60초 대기 / 키 형태·개인 키 전환) → **재개는 1번뿐**,
  그 뒤엔 진단 기록과 함께 멈춘다. 실제 빌드에서 진단→감속→재개가 한 번 돌았다.
- 캐시 TTL 6h → **48h**(먼 날짜). 한도가 24시간마다 풀리는데 6시간이면 이틀에 나눠 굽기가 불가능했다.
- 코레일 `S수서`·`여수엑스포` ↔ TAGO `수서`·`여수EXPO` 가 안 맞아 **매일 역ID 없음 2개** → 품질검사가 빌드를 버렸다.
- TAGO 가 **같은 행을 되풀이**(무궁화 1197 × 4)하고 **다음날 00시대 출발 행**을 섞어 줘서
  동대구→부산 등이 통째로 실패했다. 같은 행은 합치고 다음날 행은 버린다(시각이 다른 진짜 중복은 여전히 실패).
- `판교(경기)` 좌표를 못 찾아 매일 경로실패가 났다. OSM 판교가 둘(충남·경기)인데 조회가 엇갈렸다
  → 괄호 속 지역 상자로 고른다.
- 실패한 OD 쌍은 숫자뿐이라 무엇인지 몰랐다 → 쌍 이름·이유를 로그에 적는다.
- 09-21 은 TAGO 가 159쌍 전부 정상 응답에 **빈 목록**이다(앞뒤 날짜는 있음). 제공기관 공백으로 보고 실적 패턴으로 굽는다.
  빈 응답도 먼 날짜는 캐시 6시간 — 10분마다 159쌍을 다시 물어 빌드마다 429 를 부르고 있었다.
- **공표 시각표 기준 날짜가 0/15 였던 진짜 원인은 병합 규칙이었다.** TAGO 는 한 열차를 여러 OD 쌍에서
  주는데 '가장 긴 구간' 하나만 남기고 대조했다. 실적이 서울→부산인 열차가 행신→부산 응답과 비교돼
  **시종착 불일치 295편**. 이제 **물어본 OD 쌍 그대로의 응답**으로 대조한다.
  - 전체 OD 를 물어봤는데 없고 공표엔 짧은 구간(서울→부산)만 있으면 그 구간으로 **잘라** 맞춘다(KTX 7 등 59편). 조회가 실패한 날엔 자르지 않는다.
  - **순환 열차**(홍성→서해선→천안→장항선→홍성, 1241~1246)는 같은 역 쌍이라 물을 수 없었다 → 시발→중간역 구간으로 묻고 출발시각만 평행이동.
  - 미확인이 **1% 이하**면 그날은 공표 기준이다. 한 편(1146: 공표 81분·실적 118분)이 이상하다고 900편의 근거를 추정이라 부르지 않는다. 편별 근거는 그대로 남는다.
  - 미확인은 이제 **이유별 편수 + 예시 열차**를 빌드 로그에 적는다.
  - 결과 **공표 기준 9/15일**(09-13~09-22, 09-21 은 제공기관 공백). 09-23 이후는 TAGO 에 아직 없다 —
    '약 2주 앞'이 아니라 **약 9일 앞**까지만 올라온다(실측). 그 뒤는 실적 패턴으로 돌고 화면이 그렇게 말한다.
- **공항철도 API 는 쪽 순서가 고정돼 있지 않다**(1쪽 A2166·2쪽 A2112·3쪽 A2068). 여러 쪽을 동시에 받으면
  경계가 밀려 같은 행이 두 번 오고 그만큼 빠진다 → '중복 행/페이지'로 **421편이 통째로 빠질 뻔했다.**
  품질 가드('오늘 열차 수 25% 감소')가 막았다. 이제 한 번은 순서대로 다시 받고, 그래도 어긋나면 실패한다.
- 품질 가드 '구간별 표시 열차 급감'이 **좋아진 빌드를 막았다.** 구간 키에 근거(korail/korail-tago)가 붙어
  있어 확인율이 오르면 `korail:` 구간이 줄어든 것으로 읽혔다. 키를 운영사로만 잡고, 잘린 열차는 실적 시·종착(`evidence.trimmedFrom`)으로 센다.
- `동백산→솔안` 구간이 **점 하나**로 나와 `regress-ktrainrader24` 두 건이 빨갰다(솔안 정류장 노드가 동백산 쪽 정점에 걸림).
  길이 0 구간(두 점)으로 남기고, `validate:build` 가 점 하나짜리 구간을 배포 전에 거른다.
- 회귀 `regress-ktrainrader24` 의 고지 기억 키가 `v1` 이었다 — 앱은 v0.5.0 부터 `ktr24.notice.v2`. 검사만 틀려 있었다.

**화면**
- **종류 필터가 기본 펼침**이다(지미 지시). 좁은 화면(≤720px)은 접힘. 접고 편 상태는 브라우저가 기억한다(`ktr24.filter.v1`).
  검색어를 치는 동안은 결과가 보이게 잠시 접히고 지우면 돌아온다. `test:ui` 가 두 폭에서 기본 상태·기억·복귀를 잰다.

### v0.9.310 — 안정성 조치가 낸 흠을 막는다 (앱 v0.4.2)

v0.9.309 에서 요청 시한을 걸며 `AbortSignal.timeout` 을 썼다. 이건 빌드
타깃(es2022)보다 **늦게** 나온 물건이다 — Safari 16 / Chrome 103, 2022년
중반. 문법은 돌아가는데 이 함수만 없는 브라우저가 있다(Safari 15.4).
거기서는 TypeError 가 나고 **화면이 아예 안 뜬다.**

안정성을 높이려던 조치가 가용성을 떨어뜨린 셈이다. 없으면
`AbortController` + `setTimeout` 으로 직접 만든다. 회귀가 실제로 그 함수를
지운 채 데이터를 받아 본다.

> 지지대를 대면서 다른 지지대를 빼지 않았는지 봐야 한다. 새로 쓰는 API 가
> 빌드 타깃보다 최신이면, 그 차이만큼 못 뜨는 브라우저가 생긴다.

### v0.9.309 — 안정성: 조용한 실패를 화면에 드러낸다 (앱 v0.4.1)

이 화면의 가장 위험한 실패는 **멈추는 것이 아니라 멀쩡해 보이는 것**이다.
위치를 브라우저가 계산하므로 서버가 죽어도 열차는 계속 부드럽게 움직인다.
시각표가 어제 것이 되어도, 갱신이 끊겨도, 화면만 보면 알 길이 없다.
TAGO 쿼터가 조용히 말라 official 12/14 가 8/14 로 떨어졌던 때와 같은
성질의 문제다 — 증상이 없다.

**고친 것 다섯.**

1. **요청에 시한이 없었다.** 끊긴 연결에서 `fetch` 가 영원히 매달린다.
   기동 중이면 로딩 화면이 말없이 멈추고, 폴링 중이면 매달린 요청이
   10초마다 하나씩 쌓인다 — 한 시간이면 360개다. 20초 시한을 걸었다.

2. **폴링이 겹쳤다.** 느린 회선에서 앞 요청이 끝나기 전에 다음 것이 나갔다.
   이제 앞 요청이 살아 있으면 건너뛴다.

3. **실패해도 10초마다 계속 두드렸다.** 죽은 서버에는 소용없다.
   실패마다 간격을 2배로 늘리고 2분에서 멈춘다. 회복되면 곧바로 되돌아온다.

4. **끊긴 사실이 화면에 없었다.** `console.warn` 뿐이었다. 이제 70초 넘게
   갱신이 없으면 상단 배지가 **붉게** 바뀌고 "갱신 끊김 N분째 (표시 중인
   위치는 그동안의 추정입니다)"를 적는다.

5. **'오늘치 없음'이 너무 조용했다.** 2주치를 미리 굽는 구조라 재빌드가
   밀리면 **반드시** 오는 상태인데, 흐린 작은 글씨로 붙어 있었다. 이제
   배지가 **노랗게** 바뀌고 어느 날짜로 돌고 있는지 문장으로 적는다.

**빌드 쪽 둘.**

6. **실적이 시각표를 죽일 수 있었다.** `build:static` 의 실적 조회에
   try/catch 가 없어, 코레일이 한 번 삐끗하면 14일치를 다 구워 놓고
   마지막에 전부 버렸다. 있으면 좋은 것 하나가 정작 필요한 것을 막는
   구조다. 이제 경고만 남기고 시각표는 그대로 내보낸다.

7. **주간 재빌드가 실적을 지웠다.** `rm -rf` 로 폴더를 갈아엎는데 새 빌드에
   실적이 없으면 있던 파일이 조용히 사라졌다. 이제 지난 것을 들고 있다가
   되돌려 놓는다(열 머리글이 날짜라 오해 소지는 없다).

**회귀 8건**을 더했다. 그중 하나는 점 색까지 잰다 — 끊김 표시 CSS 는
출처별 점 색과 특정도가 같아서 파일에서 위로 올라가는 순간 조용히 진다.
규칙은 살아 있는데 점만 초록으로 남는, 제일 나쁜 상태가 된다.

### v0.9.308 — 최근 3일 실제 도착시각 (앱 v0.4.0)

열차를 누르면 정차역마다 **어제·그제·3일 전 실제 착발 시각**이 함께 나온다.
늦은 칸은 색으로 드러난다.

**왜 새로 받을 필요가 없었나.** 이미 받고 있던 데이터다. 화면의 시각표는
한국철도공사 운행정보(실적)에서 같은 요일 4회를 받아 **중앙값**을 낸 것이다.
그래서 "평소 이렇게 다닌다"까지만 말하고, "어제는 어땠나"는 중앙값을 내는
순간 지워진다. 그 지워진 값을 한 벌 더 담았을 뿐이다.

**날짜 파일에 넣지 않았다.** 이 값은 오늘 기준 과거라 14일치 어느 날짜에서
보든 같은 내용이 된다. `api/history.json` 한 벌로 공유한다(879편 199KB).
`index.json` 의 `historyDates` 에 적혀 있을 때만 화면이 받는다 — 404 로
유무를 확인하면 이 기능이 없는 배포에서 10초마다 헛요청이 나간다.

**조용히 틀어질 수 있는 곳.** 실적의 정차역 순서와 화면의 정차역 순서가
어긋나면 한 칸씩 밀린 시각이 붙는다. 숫자는 그럴듯하고 화면도 멀쩡하다.
사람 눈으로는 못 잡는다. 회귀가 25,078개 지점에서 시각표와의 차이를 재고
**중앙값 5분·90퍼센타일 20분**을 넘으면 실패시킨다(현재 2분 / 13분).
정렬은 이름으로 훑되 순서를 지킨다 — 홍성→홍성 같은 순환 운행이 있어
이름 하나로 색인하면 뒤엣것이 앞엣것을 덮는다.

**안 다닌 날은 비운다.** 평일 전용 열차는 일요일에 기록이 없다. 정시로
채우면 없는 사실을 지어내는 셈이다.

**열 머리글은 날짜다**(`09.09`). '어제·그제'로 적으면 재빌드가 밀렸을 때
나흘 전 기록을 어제라 부르게 된다. 회귀가 이것도 잰다.

**일일 갱신 액션**을 새로 뒀다(`history.yml`, 매일 05:40 KST). 위 3-1 참고.

### v0.9.307 — 차량 제원·시간대 표기·글꼴 (화면만 교체 · 앱 v0.3.0)

`api/` 는 손대지 않았다. 번들과 `index.html` 만 바뀐다.

**차량 제원.** 열차를 누르면 상세에 차량 형식이 나온다. Flightradar24 가
편명을 눌렀을 때 기종을 보여주는 것과 같은 자리다. 다만 성격이 다르다 —
ADS-B 에는 기체 등록번호가 실려 오지만, **한국 공개 API 어디에도
편성번호가 없다.** 그래서 개체가 아니라 **형식**을 보여 준다. 형식은
TAGO `traingradename` 이 'KTX-산천(A-type)' 처럼 구분해 준다.

제원표만 남고 "형식 기준이라 당일 실제 투입 차량은 알 수 없다"는 단서가
빠지면, 사용자는 이 값을 그날 그 열차의 사실로 읽는다. 그 쪽이 더 위험해서
회귀가 **제원표와 단서 문장을 같이 잰다**(`차량 제원이 형식 기준임을 밝힌다`).
도감 자체는 `web/src/rollingstock.ts` 에 있다.

**시간대 표기.** 버전 줄의 데이터 시각에 `KST` 를, 시계 라벨에 `(GMT+9)` 를
붙였다. 해외에서 열면 화면의 숫자가 현지 시각인지 한국 시각인지 알 길이
없었다. 회귀 2건으로 고정했다.

**글꼴.** 본문을 원티드 산스 가변체로 바꿨다(jsDelivr `@import`).
`@import` 는 규칙보다 앞에 있어야 하므로 `style.css` 맨 위에 둔다 —
아래로 내리면 브라우저가 통째로 버린다.

**앞선 화면 묶음도 함께 나간다** (별도 번호를 붙이지 않았다):
필터 들여쓰기, 오늘 총편수 숨김(운행 중만 표시), 라벨 다중 링 배치,
UI 여백, 선택 시 다른 열차 채도·투명도 낮춤과 정차역 강조,
열차가 서지 않는 역 숨김.

### v0.9.306 — 체크박스 필터·역 표시·라벨 겹침 해결 (화면만 교체)

- **필터를 체크박스로.** 눌러서 취소선이 그어지는 방식은 "지금 켜져 있나
  꺼져 있나"를 한눈에 못 알아본다. 등급 상자는 그 급 전체를 함께 켜고 끄고,
  일부만 켜져 있으면 **중간 상태**로 보인다.
- **숫자는 지금 달리는 편수다.** 하루 총편수를 적으면 지도에 12편이 떠
  있는데 219라고 적히는 꼴이 된다. 총편수는 `/219` 로 흐리게 옆에 둔다.
  매 프레임이 아니라 1초에 한 번만 갱신한다 -- DOM 을 60fps 로 다시 그리면
  스크롤과 초점이 날아간다.
- **지도에 역을 표시한다.** 선로만 있고 역이 없으면 열차가 어디쯤인지
  지명으로만 짐작해야 했다. 역이 1,119개라 이름을 다 쓰면 못 읽으므로
  **오늘 그 역에 서는 열차 수**로 중요도를 매겨 큰 역부터 붙인다 --
  별도 등급 데이터가 필요 없고 운행이 바뀌면 저절로 따라간다.
- **라벨이 열차 아이콘을 덮던 것을 고쳤다.** 겹침 검사가 라벨끼리만 봤다.
  이제 아이콘도 자리로 잡고, 오른쪽이 막히면 왼쪽·위·아래로 여덟 자리를
  돌려 가며 놓는다. 떨어져 놓인 라벨은 가는 선으로 이어 준다.
- 초기 화면에서 제주와 남해 먼바다를 뺐다.

**회귀 64 → 68건.**

### v0.9.305 — 고지 문구·종류 필터 (화면만 교체)

- **"관련 기관에 민원을 제기하지 마십시오. 정확성을 책임지지 않으니 맹신하지
  마십시오."** 사용자가 직접 요구한 문구다. 하단 고지만으로는 눈에 안 띄어
  첫 방문 안내창으로도 띄운다 — 한 번 확인해야 넘어간다(브라우저에 기억).
  ⚠️ **문구를 바꾸면 `NOTICE_KEY` 뒤 숫자를 올려라.** 안 그러면 이미 확인한
  사람에게 새 내용이 영영 안 닿는다.
- **열차 종류 필터.** 등급(모양)만으로는 ITX-마음과 무궁화호를 못 가른다.
  종별을 낱낱이 세우고 켜고 끌 수 있게 했다. 등급으로 묶어 보여 주므로
  등급 줄을 누르면 그 급 전체가 함께 꺼진다.
  실데이터에서 10종이 잡힌다 — KTX-산천 219 · KTX 156 · ITX-마음 74 ·
  KTX-이음 70 · ITX-새마을 46 · ITX-청춘 36 · 누리로 20 · 새마을호 18 ·
  KTX-청룡 4 · 무궁화호 164.
- 하단 고지가 두 줄이 되어 범례·줌 컨트롤이 가려지던 것을 함께 고쳤다.
- 초기 화면 범위에서 제주와 남해 먼바다를 뺐다. 철도가 없는 곳까지 넣어
  맞추니 한반도가 화면의 절반만 차지했다.

**회귀 51 → 64건.** 고지 문구·필터 동작·버전 표시를 못 박았다.

---

⚠️ **v0.9.304 는 데이터를 다시 굽지 않았다.** TAGO 일일 요청 한도(10,000건)를
넘겨 그날은 재빌드가 불가능했다. 대신 옛 파일의 `label` 에 TAGO 원본 종별명이
남아 있는 것을 이용해(`KTX-산천(A-type) 10`) 화면에서 등급을 되살렸다
(`web/src/data.ts` 의 `normalizeTrain`). 다음 재빌드부터는 `grade` 가 파일에
직접 들어가므로 이 경로는 옛 배포본을 위한 안전장치로만 남는다.

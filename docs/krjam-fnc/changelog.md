# /krjam-fnc — 제16회 한국잼버리 급식편의본부 (변경 이력)

> 국문: **제16회 한국잼버리 급식편의본부** · 영문: **The 16th KNJ Food Service & Convenience Division**
> Vanilla HTML/CSS/JS. 엔트리: `krjam-fnc.html` · 모듈/자산: `krjam-fnc/`. 라우트 `/krjam-fnc`.
> 목적: 급식편의본부 운영요원(IST) 오리엔테이션 자료(**현재 31쪽** — IST 업무배정표 3쪽은 삭제, §19.2·§19.3)를 **플립북**으로 열람.

### 19.1 v0.9.234 — 신규 서비스: 오리엔테이션 자료 플립북

**사용자 지시**: "서브페이지를 하나 더 추가. 국문 `제16회 한국잼버리 급식편의본부` / 영문 `The 16th KNJ Food Service & Convenience Division`. 첨부 PDF 를 플립북으로 만드는 게 목표."

**확정 사항**(AskUserQuestion): 라우트 `/krjam-fnc` · 페이지는 **PDF 원본 이미지 그대로**(디자인 100% 보존) · 루트 랜딩 카드에는 **노출하지 않음**(링크 아는 사람만) · 부가기능 **목차·전체화면/확대·PDF 원본 내려받기·페이지 딥링크** 전부.

**자료 렌더**(원본 `급식편의본부 오리엔테이션_V7-1 OT최종.pdf`, 34쪽 960×540pt = 16:9)
- 본문: `pdftoppm -png -r 144`(=1920×1080, 2배) → `cwebp -q 84 -sharp_yuv -m 6` → `krjam-fnc/pages/p01..p34.webp` (합 4.3MB).
  가장 조밀한 IST 업무배정 표(31~33쪽)도 100% 배율에서 판독되고, 확대 시 240%까지 선명.
- 썸네일: `-r 24`(320×180) → `-q 72` → `krjam-fnc/thumbs/t01..t34.webp` (합 316KB, `loading="lazy"`).
- 원본 PDF 동봉: `krjam-fnc/assets/KNJ16-FnC-Orientation.pdf`(다운로드 시 한글 파일명으로 저장).
- OG 1200×630: `krjam-fnc/assets/og-fnc.png` — §18.22 와 동일 방식(HTML 템플릿 + 헤드리스 Chrome, 엠블럼 base64, 잼버리 라벤더 톤).

**뷰어**(`krjam-fnc/app.js` · `krjam-fnc/styles.css`, 의존성 0 · 빌드 없음)
- **페이지 넘김**: 3D `rotateY` 책장 넘김(원점 = 왼쪽 모서리, 0→-95°, 480ms). 90°를 넘기면 `backface-visibility:hidden` 으로 사라져 **뒷면이 좌우 반전돼 보이는 문제가 없다**(그때 아래 장은 이미 다 드러난 상태). `prefers-reduced-motion` 이면 애니메이션 없이 교체.
- 조작: 좌우 버튼 · ← → PageUp/Down Space Home End · 스와이프 · 휠 · 하단 스크러버(드래그 중엔 애니메이션 없이 즉시 이동).
- **목차**: 34쪽 썸네일 + 쪽 제목(코드 내 `TITLES`). PC 우측 패널 / 모바일 하단 시트. 현재 쪽 강조·자동 스크롤. `T` 토글, `Esc` 닫기.
- **확대**: 더블클릭·더블탭 240% 토글, 핀치, `Ctrl/⌘+휠`, `+ - 0` 키. 드래그 팬(경계 클램프). 확대 중엔 스와이프 넘김 대신 팬.
- **가로 보기**(좁은 세로 화면 전용 버튼): 16:9 슬라이드를 90° 돌려 화면을 채운다 — 390px 폭 기준 **1.85배** 크게 읽힌다. 회전 중엔 좌표계가 어긋나므로 확대는 잠그고 넘김만 남긴다. 가로로 돌리면 자동 해제.
- 전체화면(`F`) · PDF 원본 내려받기 · **딥링크 `#p12`**(넘길 때 `replaceState`, 진입 시 복원, 범위 밖이면 1쪽).

**작업 중 실제로 잡은 버그 3건**(전부 회귀에 고정)
1. **`[hidden]` 이 무력화돼 목차 패널이 화면을 덮고 클릭을 가로챔** — `.toc{display:flex}`(클래스 선택자)가 UA 의 `[hidden]{display:none}` 을 이긴다. 상단바·넘김 버튼 클릭이 전부 썸네일로 들어가 엉뚱한 쪽으로 점프했다. → 전역 `[hidden]{display:none!important}` + `#leafUnder`·`#leafFlip` 에 초기 `hidden` 속성.
2. **썸네일이 세로로 늘어나 좌우가 잘림** — `img` 의 `height="180"` 속성이 살아 있고 CSS 가 `width` 만 지정하면 **양쪽이 확정되어 `aspect-ratio` 가 무시된다**. → `.tocitem img{height:auto}`.
3. **가로 보기 확대 효과가 반감** — `.book` 이 flex 아이템이라 기본 `flex-shrink:1` 로 지정 폭이 스테이지 폭까지 눌렸다(회전 전 기준 축소). → `.book{flex:none}`.

**검증**: `node --check` + 신규 회귀 `test/regress-krjam-fnc.js` **48/48 통과**(실제 Chrome 헤드리스, 로컬 정적 서버) — 자산 34+34+PDF+OG 존재/200 · 로딩 · 넘김(버튼·키보드·이미지 실제 교체·해시) · 스크러버 · 목차(16:9·활성·이동·Esc) · 확대 · 딥링크(#p12, 범위 밖) · 모바일(레이아웃·터치타깃 40px·하단시트·가로 보기 전환/배율/확대잠금) · **콘솔 에러 0 · 요청 실패 0**. 서버/KV 무접촉(정적 자산만).

**주의**: 자료에 **운영요원 실명 업무배정표**가 포함되어 있다. 루트 랜딩에 노출하지 않고(`noindex`), URL 공유로만 접근하는 현 방침을 유지할 것.

### 19.2 v0.9.235 — 31쪽 삭제 · 목차 좌측 상시 사이드바 · 챕터 목차 · 엠블럼

**사용자 지시**: "31페이지는 삭제해달래. 한국잼버리 관련페이지나 로고도 그거 사용해줘" → 이어서 "목차는 좌측에 보이게 해주고, 적절히 이미지 파일들을 읽고 목차 페이지를 만들어줘. 모바일에서도 최적화 되게".

**① 31쪽 삭제 (IST 업무배정 — 8/3 · 8/4 · 8/5)**
- `pages/p31.webp` · `thumbs/t31.webp` 제거 후 **32~34 → 31~33 재번호**, `TOTAL` 33.
- **내려받기 PDF 도 같이 재생성**해야 한다 — 뷰어에서만 감추면 `assets/*.pdf` 를 받는 순간 그대로 보인다. `pdfseparate` → 31번 제외 → `pdfunite` → `pdftocairo -pdf`(폰트 중복 정리). 결과 33쪽.
  - ⚠️ 크기는 2.1MB → **6.3MB**. poppler 재출력은 페이지별로 폰트를 다시 심어 원본만큼 압축되지 않는다(원본 34쪽을 그대로 재출력해도 5.7MB). `qpdf`·`gs`·PyObjC(Quartz) 모두 이 환경에 없어 더 줄일 방법이 없었다. 이미지로 다시 만들면 23MB 라 더 나쁘다. PDF 버튼 툴팁에 "약 6MB" 표기.
  - 검증: 새 PDF 전문에 구 31쪽 고유 문자열(`8월4일(7-9)`) **0건**, 새 31쪽 = 8/6·8/7 배정표.
- 딥링크 `#p34` 는 범위 밖이 되어 1쪽으로 폴백(회귀에 고정).

**② 목차를 좌측으로 + 챕터 구조**
- 레이아웃 재구성: `shell > topbar + body(toc + main(stage + bottombar))`. 넓은 화면(≥1024px)에서 목차는 **오버레이가 아니라 레이아웃의 일부**(폭 292px, 1500px+ 에선 330px) — 접으면 뷰어가 그만큼 넓어진다.
- 항목을 그리드 → **행(썸네일 76px + 번호 + 제목 2줄 clamp)** 으로. 각 슬라이드를 실제로 열어 확인한 뒤 **부서/주제 전환 지점 기준 9개 챕터**(`CHAPTERS`)로 묶고 sticky 머리글을 붙였다: 행사·본부 개요(1–7) / 급식편의지원부(8) / 식자재보급운영부(9–11) / 잼버리식당·식사 제공(12–15) / 편의시설운영부(16–20) / 안전교육·입퇴영(21–26) / 운영요원 안내(27–29) / 업무 일정·IST 배정(30–32) / 맺음말(33).
- 쪽 제목도 슬라이드 실물 기준으로 다듬음(예: 13쪽은 머리말이 '식자재보급운영부'지만 내용은 운영요원 식사 제공 안내 → "식사 제공 안내 — 조식·중식(간편식)·석식").
- 열림 상태 기억(`localStorage: krjam-fnc:toc`). **넓은 화면에서만 저장**한다 — 드로어 여닫음까지 저장하면 다음 방문에 드로어가 열린 채 떠서 뷰어를 덮는다(회귀가 실제로 잡아냈다: 데스크톱에서 저장한 '열림'을 모바일이 물려받았다).
- PC 는 항목을 눌러도 목차가 닫히지 않는다(연속 탐색). `Esc` 는 드로어만 닫는다.

**③ 모바일 최적화**
- 좁은 화면(≤1023px)에서는 **좌측 드로어**(폭 min(330px, 86vw)) + 배경 마스크, 기본 닫힘. 항목을 누르면 닫힌다.
- 터치 타깃 ≥40px(항목 패딩 확대), 썸네일 84px, 제목 13px 유지.

**④ 한국잼버리 로고**
- 상단바 좌측에 **공식 엠블럼**(`/jamboree/assets/logo.png`) 표시 + 제목 묶음을 브랜드 링크로. favicon·OG 는 이전부터 같은 엠블럼/잼버리 톤을 쓰고 있었고, OG 문구만 33쪽으로 재렌더.

**검증**: 회귀 `test/regress-krjam-fnc.js` **65/65 통과**(48 → 65로 확장). 추가된 항목: 34번째 파일 부재 · 내려받기 PDF 33쪽 · 목차 좌측 배치/마스크 없음/챕터 9개/접기 시 뷰어 확장/상태 저장 · 31쪽 내용 교체 확인 · `#p34` 폴백 · 모바일 드로어(좌측·마스크·터치타깃·선택 후 닫힘·Esc) · 엠블럼 로드. 콘솔 에러 0 · 요청 실패 0.

### 19.3 v0.9.236 — IST 업무배정표 전량 삭제(3쪽) · 배포 순간 화면 깨짐 원인

**사용자 지시**: "IST업무 배정은 모두 다 삭제해줘. 지금 31페이지 32페이지도 마찬가지" (+ "갑자기 왜 다 이렇게 깨지냐" · "목차를 열면 깨지네").

**① IST 업무배정표 3쪽 전량 삭제 → 31쪽**
- 원본 34쪽 기준 31·32·33쪽(8/3~8/5 · 8/6~8/7 · 8/8~8/9 배정표)이 전부 대상. §19.2 에서 1쪽만 지웠던 것을 나머지 2쪽까지 확대.
- 이미지/썸네일 삭제 후 맺음말이 31쪽으로. `TOTAL` 31, `TITLES` 31개, `CHAPTERS` 는 마지막 두 챕터를 **'업무추진 일정 · 마무리'** 로 합쳐 8개.
- **PDF 는 원본에서 한 번에 3쪽을 빼 재생성**(§19.2 의 33쪽 결과물을 또 자르면 재출력이 누적된다) → 31쪽 6.2MB. 검증: `업무배정`·`8월4일(7-9)`·`8월6일(7-10)`·`8월8일(7-10)` 전부 **0건**, 마지막 쪽 = 맺음말.
- 이로써 **자료에서 실명 업무배정표가 완전히 사라졌다**(남은 실명은 5쪽 조직도). 랜딩 미노출 방침은 그대로 유지.

**② "갑자기 깨진다"의 원인 — 배포 전파 순간의 구 HTML + 신 CSS**
- 라이브 CSS/JS 를 로컬과 바이트 단위로 대조(동일) 하고 5개 뷰포트(1280×648 / 1440×900 / 2370×1200 / 1512×800 / 1280×560)에서 지오메트리를 찍어 **전부 정상**임을 확인했다. 즉 코드 문제가 아니다.
- 증상(상단바 → 얇은 뷰어 띠 → 하단바 → 그 아래 좌측 목차)은 **v0.9.234 HTML + v0.9.235 CSS** 조합에서만 나온다: 구 HTML 은 `toc` 가 `shell` 의 **마지막 자식**이라, 신 CSS 의 `.toc{flex:none;width:292px}` 이 세로 flex 의 마지막 칸으로 잡히며 `.stage{flex:1}` 을 납작하게 눌러버린다.
- 배포 직후 몇 초간 엣지 노드별로 구/신 배포가 섞여 서빙되는 구간이 실제로 관측됐다(같은 시각 `/krjam-fnc/app.js` 가 HTML 본문을 반환). `version-watch.js` 의 자동 새로고침이 그 구간에 걸리면 이 조합이 만들어진다.
- **문서는 `Cache-Control: no-cache`(확인)** 라 **새로고침 한 번이면 복구**된다. 정적 자산은 `max-age=14400` 이지만 HTML 이 항상 신선하므로 `?v=` 로 갱신된다.
- 교훈: **DOM 구조를 바꾸는 배포는 구 HTML + 신 CSS 조합이 잠깐 존재할 수 있다.** 배포 직후 검증은 최소 20~30초 뒤에, 그리고 "깨졌다"는 제보를 받으면 먼저 강제 새로고침(⌘⇧R)으로 재현되는지부터 확인할 것.

**검증**: 회귀 **67/67 통과**(65 → 67). 추가: 삭제된 32~34번째 파일 부재 · PDF 31쪽 · 목차/챕터에 '업무배정' 문자열 0건 · `#p32` 폴백 · 30쪽=업무추진 일정 · 31쪽=맺음말.

### 19.4 v0.9.237 — 모바일 '이어보기'(세로 스크롤) 모드

**사용자 지시**: "모바일에서 봤을때에는 내용을 직접 클릭해서 넘기는형식도 좋지만, 슬라이드로 위아래 할 수 있는 건 어때?"
**확정**(AskUserQuestion): **문서처럼 죽 이어보기**(스냅 없는 연속 스크롤) · **모바일·터치에만** 적용(PC 는 책장 넘기기 유지).

- **피드**(`#feed`): 31쪽을 세로로 쌓고 쪽마다 `번호 + 제목` 캡션. 이미지 `loading="lazy"` + 1920×1080 속성으로 자리 예약. 최대 폭 900px(태블릿).
- **현재 쪽 판정**: 스크롤 시 rAF 로 **상단 기준선(위에서 min(64px, 25%))에 걸린 첫 쪽**을 현재 쪽으로. 하단 표시·스크러버·목차 강조·해시가 그대로 연동된다.
  - ⚠️ 처음엔 '화면 중앙' 기준으로 짰다가 실패 — 세로 화면엔 한 번에 3쪽 가까이 보여서, 목차로 20쪽을 열면 중앙엔 21~22쪽이 걸려 **현재 쪽이 곧바로 튀었다**. 문서 뷰어 관습대로 상단 기준으로 바꿔 해결.
  - ⚠️ `el.offsetTop` 은 `offsetParent` 가 무엇이냐에 따라 기준이 달라져 스크롤 위치가 어긋났다(`.feed` 에 `position:relative` 가 없었음). **스크롤 컨테이너 기준 좌표는 rect 차이로 계산**(`feedTopOf`) + `.feed{position:relative}`.
- **확대**: 커스텀 줌 대신 **브라우저 기본 핀치줌**에 맡긴다(`.stage` 와 달리 `touch-action` 을 막지 않음). 이어보기 중엔 '가로 보기' 버튼을 숨긴다.
- **모드 전환**: 하단 바 우측 버튼(터치 기기에만 노출)으로 `이어보기 ↔ 한 장씩(플립북)`. 보던 쪽을 유지하고 `localStorage: krjam-fnc:mode` 에 기억. 터치 기본값은 이어보기.
- 목차·스크러버·키보드·딥링크는 두 모드에서 동일하게 동작(이어보기에선 해당 쪽으로 스크롤).

**검증**: 회귀 **82/82 통과**(67 → 82). 추가: 터치 기본 모드 · 세로 배치/스크롤 가능 · 캡션 · 스크롤→현재 쪽/해시 갱신 · 목차/스크러버로 스크롤 이동 · `touch-action` 미차단 · 모드 전환 시 쪽 유지/저장 · PC 는 피드·모드버튼 숨김.

### 19.5 v0.9.239 — 진입 연출 · 살아있는 배경 · 로딩 표시

**사용자 지시**: "홈페이지에 조금 화려한 애니메이션들을 줄 수 있나?" + "로딩중이면 로딩중인 중간 로딩 바라던지 이런것들을 포함해서".
**확정**(AskUserQuestion): 대상 = **랜딩 `/` + `/krjam-fnc` 둘 다**, 강도 = **배경까지 살아있게**.
> [ui-ux.md](../../rules/ui-ux.md) 의 '절제' 원칙과 긴장이 있는 선택이라 질문에 그 점을 적어 확인받았다. 구현은 **느리고 은은하게**(20~44초 주기), 색은 기존 브랜드 팔레트 안에서, `prefers-reduced-motion` 이면 전부 정지.

**`/krjam-fnc`**
- **부팅 로딩 화면**: 엠블럼(맥박) + **진행바** + 문구. 실제로 기다리는 것에 맞춰 단계를 올린다 — 시작 22% → 글꼴 준비(`document.fonts.ready`) 55% → 첫 장 이미지 100%. 그 사이엔 90%까지 천천히 기어가 멈춰 보이지 않게. 4초 안전 타임아웃.
  - 엠블럼(117KB)이 늦게 오면 로딩 화면이 비어 보여 `<link rel=preload fetchpriority=high>` + 원형 플레이스홀더 배경.
- **넘김 진행바**(`#topload`): 다음 장이 캐시에 없을 때만. 캐시된 넘김에서 깜빡이지 않도록 **120ms 지연 후** 켜고 도착 즉시 끈다.
- **이어보기 스켈레톤**: 아직 안 내려온 쪽은 16:9 자리에 시머가 흐르고(캡션은 먼저 보인다), 도착하면 그 자리에서 이미지가 페이드인.
- **배경**: 스테이지 뒤에 아주 느린 두 겹(30s·38s). 이어보기(피드가 화면을 덮음)에서는 `display:none` 으로 아예 끈다.
- **진입**: 상단바 → 목차 → 본문 → 하단바 순차 등장.
  - ⚠️ **`.book` 은 opacity 만 애니메이션한다.** `transform` 을 넣었더니 `animation-fill-mode:both` 가 끝난 뒤에도 그 값이 남아 **'가로 보기'의 `rotate(90deg)` 를 덮어써서** 회전이 죽었다(회귀가 잡음).

**`/` 랜딩**
- 배경: 느리게 표류하는 그라데이션 3겹(32~44s) + 스카우트 매듭을 연상시키는 선 3줄(`stroke-dashoffset` 흐름, opacity .16). `pointer-events:none` 으로 클릭 방해 없음.
- 로고 마크: 그라데이션 흐름 + 6.5초마다 지나가는 빛. 카드: 순차 등장(stagger) · 호버 시 4px 상승 + 빛 스윕 + 아이콘 회전 · 유리질감(backdrop-filter).
- **이동 로딩 표시**: 카드를 누르면 상단 진행바가 차오르고 누른 카드에 로딩 상태(아이콘 회전 + 하단 바 흐름). 뒤로가기(bfcache)로 돌아오면 자동 해제.

**성능**: 움직이는 것은 `transform`/`opacity`/`background-position` 뿐이라 레이아웃·리페인트가 없다. blur 는 정적으로 한 번만 걸린다.

**검증**: `/krjam-fnc` 회귀 **89/89**(82 → 89: 부팅 진행바·상단 로딩바·배경 2겹·배경이 뷰어를 안 가림·스켈레톤 걷힘/유지·이어보기에선 배경 off), **신규 `test/regress-landing.js` 15/15**(카드 5개·링크·연출 후 가시성·클릭 가능·배경 3겹+매듭 3줄·pointer-events·가로 스크롤 없음·로딩바 표시·모바일 1열/터치타깃·모션 줄이기에서 정지 + 내용은 그대로). 기존 회귀(planning 132·nav 19·jebo 29·server 16) 이상 없음.

### 19.6 v0.9.240 — 랜딩 이펙트 강화 (사용자: "좀 화려한 이펙트들이 이래저래")

v0.9.239 연출이 너무 은은하다는 피드백. 랜딩(`index.html`)만 대폭 강화했다. *(플립북은 읽는 화면이라 그대로 둔다.)*

**추가된 것**
- **오로라 2겹**: 콘틱 그라데이션 색면이 46s·64s로 회전(`mix-blend-mode:multiply`).
- **모닥불 불티**: 작은 점들이 화면 아래에서 위로 13~24초에 걸쳐 떠오른다. PC 24개 / 모바일 14개(JS 생성, 모션 줄이기면 아예 생성하지 않음).
- **그레인**: `feTurbulence` 데이터 URI 한 장(정적), opacity .05 — 색면이 밋밋해지지 않게.
- **타이틀**: 글자 단위 등장 + 그라데이션이 흐르는 텍스트.
- **카드**: 커서를 따라가는 **3D 기울기**(rotateX/Y, 마우스 기기에서만·rAF 스로틀) + **커서 위치 광원** + **뒤에서 도는 무지개 후광** + 훑고 지나가는 빛 + **카드별 아이콘 모션**(핀 튐 / 확대 / 흔들림 / 한 바퀴 / 맥박).
- 배경 색·선 대비도 전반적으로 올림(blob opacity .5→.62, 매듭선 .16→.3).

**잡은 버그 3건**(전부 회귀에 고정)
1. **빛 줄기가 카드 밖으로 샘** — `.card>*:not(.ring)` 이 `.sheen` 까지 `position:relative` 로 덮어, `.sheen` 이 **크기 0인 인라인 박스**가 되면서 `overflow:hidden` 이 무력화됐다. → 선택자에서 `.sheen` 도 제외.
2. **후광이 위아래 카드까지 길게 번짐** — `.ring` 이 **요소 자체를 `rotate()`** 해서 회전한 사각형의 대각선만큼 박스가 커졌다(측정: 141px 카드 → 439px 박스). → `@property --ang` 로 **그라데이션 각도만** 회전. 미지원 브라우저는 정지한 후광으로 자연스럽게 낮아진다. 겸사겸사 `.card:hover{z-index:3}` 로 호버 카드를 항상 위로(안 그러면 앞 카드 위로만 번져 방향에 따라 다르게 보인다).
3. **호버 시 본문 대비 미달** — 후광이 `backdrop-filter: saturate(1.3)` 를 타고 진하게 배어 나와 **실측 4.45**([ui-ux.md](../../rules/ui-ux.md) 기준 4.5 미달). → 호버에서 채도 1.02·바탕 .97·본문색 강화 → **4.94**.

**검증**: `test/regress-landing.js` **29/29**(15 → 29). 새 항목: 오로라·그레인·불티 개수(PC 24 / 모바일 14 / 모션줄이기 0) · 타이틀 글자 가시성 · 카드별 후광·시트 요소 · **커서 기울기/광원 갱신과 복귀** · **후광이 카드에 붙어 있는지** · **호버 카드 z-index** · **호버 상태 본문 대비를 실제 픽셀로 측정해 4.5 이상**(스크린샷 → 페이지 내 canvas 디코드, 추가 의존성 없음) · 모바일 기울기 미적용. 나머지 회귀(fnc 89 · jebo 29 · nav 19) 이상 없음.

### 19.7 v0.9.241 — 랜딩 텍스트 가시성 긴급 수정

**사용자 지적**: "화려한건 좋은데 텍스트 가시성은 확보해야지 제목의 텍스트 가시성이 박살나면 어떻게하냐" — 정당한 지적이고, v0.9.240 에서 내가 넣은 회귀다.

**실측으로 원인 확인**(요소 영역 캡처 → 페이지 내 canvas 디코드 → 최명/최암 픽셀 명암비)
- **제목**: 그라데이션 정지점에 밝은 보라(`#8B5CF6`)·파랑(`#3D8FD1`)을 넣어, 흐름이 그 구간을 지날 때 **2.63:1** 까지 떨어졌다(주기 8초를 1초 간격으로 샘플링해 확인). → 정지점을 **전부 어두운 값**(`#3D1D6E · #5B2EA6 · #1F4A80`)으로 교체 → **최저 5.78:1**.
- **부제**(`.sub`): `--muted(#655e73)` → 움직이는 색면 위에서 **4.14** → `#494256` → **6.41**.
- **푸터 링크**: `--faint(#948da2)` → **2.22**(12px 링크라 4.5 필요) → `#4d475c` → **6.17**. 이건 v0.9.240 이전부터 있던 값인데, 배경이 진해지면서 드러났다.

**교훈**: 배경이 움직이거나 글자 자체가 그라데이션이면 **눈으로는 못 잡는다**. 색을 얹기 전에 실측하고, 애니메이션이 있으면 **주기 전체를 샘플링**해야 한다.

**검증**: `test/regress-landing.js` **34/34**(29 → 34). 공용 `contrastOf(page, selector)` 헬퍼를 두고 제목(주기 5회 샘플링·최저값)·부제·카드 제목·카드 본문·푸터 링크를 매번 실측한다. 호버 상태 본문 대비 검사도 이 헬퍼로 통합.

### 19.8 v0.9.242 — 랜딩 진정시키기 (사용자: "여기 너무 정신이 없는데?")

v0.9.240 에서 한 번에 너무 많은 것을 움직였다. **동시에 움직이는 것의 수와 배경의 농도**를 낮춰, 화려함은 남기되 내용이 앞으로 나오게 했다.

| 요소 | 이전 | 지금 |
|---|---|---|
| 오로라 | opacity .62 | **.34** (blur 64) |
| 색덩어리 | .62 | **.38** (blur 70) |
| 매듭선 | .3 · 1.8px · 18~24s | **.11 · 1.2px · 30~38s** |
| 불티 | PC 24 / 모바일 14, 밝기 .9 | **PC 10 / 모바일 6**, 밝기 .5, 더 작고 느리게(18~31s) |
| 그레인 | .05 | **.035** |
| 제목 | 그라데이션이 계속 흐름 | **정지**(그라데이션은 유지) — 움직이는 글자가 가장 산만했다 |
| 카드 후광 | 무지개(로즈 포함) .72 | **브랜드 계열** .42 |

호버 상호작용(기울기·광원·후광·아이콘 모션)은 **사용자가 부를 때만** 나오므로 그대로 뒀다. 배경 농도가 낮아지면서 텍스트 대비는 오히려 전반적으로 올라갔다(부제 6.41→7.29 · 카드 본문 5.68→5.82 · 푸터 6.17→6.93).

**교훈**: "화려하게"는 **강도**를 올리라는 뜻이 아니라 **완성도**를 올리라는 뜻에 가깝다. 상시 움직이는 레이어는 적게, 상호작용에 붙는 모션은 풍부하게.

### 19.9 v0.9.245 — 완료 정의 적용 중 잡힌 2건

사용자와 확정한 [완료 정의](../../rules/handoff.md)를 그대로 돌리다가 나온 것들. **둘 다 "검증 자체가 못 미더웠던" 문제**다.

**① 터치 타깃 미달 — 브랜드 링크 36px**
- 라이브 접근성 실측(전 요소 스윕)에서 `/krjam-fnc` 상단 `.brand`(엠블럼+제목, `/` 로 가는 링크)가 모바일에서 **36px**로 나왔다. 엠블럼 30 + 패딩 6. 개별 버튼만 보던 기존 검사는 이걸 못 봤다.
- 조치: `.brand{min-height:40px}`. 회귀에 **"모바일 조작 요소 전부 40px 이상"**(보이는 `a`·`button` 전수 스윕)을 추가 → 90/90.

**② 회귀가 간헐적으로 터짐 — `screenshot({clip})` 이 뷰포트를 벗어남**
- `contrastOf()` 가 요소 위치를 그대로 clip 으로 넘겨, 카드가 화면 아래로 걸치면 Puppeteer 가 예외를 던졌다(3바퀴 중 1회).
- 조치: 재기 전에 `scrollIntoView` 하고 **뷰포트 안으로 잘라서** 잰다. 8회 연속 통과 확인.

**③ 검증 스크립트의 거짓 통과 — `$?` 가 `tail` 을 읽고 있었다**
- `out=$(node ... | tail -1); rc=$?` 는 파이프의 **마지막 명령(tail)** 종료코드를 본다. 그래서 테스트가 터져도 `✓` 로 찍혔다. 실제로 한 바퀴가 거짓 통과했다.
- 조치: 출력을 파일로 받고 `node` 자체 종료코드로 판정. **검증 도구를 믿기 전에 검증 도구부터 검증할 것.**

### 19.10 v0.9.246 — 랜딩 접근성 2건 (전수 스윕으로 잡음)

라이브 접근성 실측에서 랜딩만 기준 미달로 남아 있었다. 플립북·보드에는 전수 스윕 검사가 있었는데 **랜딩에는 없어서** 그동안 안 보였다.

- **폰트 하한 위반**: 카드 칩(`.tag`) **11.5px**, 푸터 **12px** → 둘 다 **13px**([ui-ux.md](../../rules/ui-ux.md) 최소 13px).
- **터치 타깃 위반**: 푸터 링크(Tour Admin · Privacy policy)가 **16px** 높이 → `display:inline-flex; min-height:40px; padding:0 8px` 로 40px 확보(간격도 14→6px 로 줄여 붙어 보이지 않게).
- **회귀에 고정**: 랜딩에도 **보이는 모든 요소 전수 스윕**(폰트 13px+ · `a`/`button` 40px+)을 넣었다 → 36/36.

**교훈**: 같은 규칙이라도 **화면마다 검사가 있어야 지켜진다.** 새 화면을 만들면 그 화면용 전수 스윕부터 넣을 것.

### 19.11 v0.9.247 — 화면·해상도마다 전수 스윕 (마지막 사각지대 제거)

라이브 실측을 **4개 조합(랜딩/플립북 × 모바일/PC)** 으로 늘리자 플립북 **PC 에서만** 3건이 남아 있었다. 모바일에서는 목차가 닫혀 있어 안 보였던 것.

- `.tocsec-h`(챕터 머리글) **12.5px** → 13px · `.tocitem .n`(쪽 번호) **12.5px** → 13px(폭 20→22px)
- `The 16<sup>th</sup>` 의 첨자가 **9.36px**(13 × .72em) → 첨자를 없애고 `The 16th` 평문으로. 13px 로 키우면 첨자로 보이지 않아 의미가 없다.

**사각지대를 구조적으로 메웠다** — 스윕이 있는 곳에서만 규칙이 지켜지므로:
- 플립북 회귀: 모바일만 보던 것을 **PC 스윕 추가**(92/92)
- 랜딩 회귀: 모바일만 보던 것을 **PC 스윕 추가**(38/38)
- planning 모션 회귀: PC 1440 만 보던 것을 **모바일 390 스윕 추가**(25/25) — 위반 0건이었지만 이제 고정됐다

**교훈**: 같은 규칙이라도 **화면 × 해상도** 조합마다 검사가 있어야 한다. 한 조합에서 통과했다고 다른 조합이 안전한 게 아니다(목차·사이드바처럼 특정 폭에서만 나타나는 UI 가 있다).

### v0.9.253 — 플립북 → 운영 안내 보드 (사용자: "홍보부처럼 기능과 버튼이 살아있게")

`/krjam-fnc` 를 **PDF 를 넘겨 보는 화면에서 "현장에서 찾아 쓰는 화면"**으로 바꿨다. 원본 자료는 없애지 않고
`/krjam-fnc-book` 으로 옮겨 그대로 살려 뒀다 — 자료 원본을 확인해야 할 때가 반드시 온다.

**화면 9개** — 대시보드 / 본부·조직 / 급식 운영 / 식사 메뉴 / 편의시설 / 입영·퇴영 / 운영요원 안내 / 업무 일정 / 자료 원문.
좌측 내비(모바일은 하단 탭 5개) · 상단 검색 · 각 섹션마다 **원문 몇 쪽인지** 링크.

**살아 있는 것**
- 입영·개영·퇴영 **실시간 카운트다운**, 상단 현재 시각.
- **지금 식사 운영 중** 여부를 현재 시각으로 계산해 조·중·석식마다 예정/운영 중/종료 표시.
- **오늘 본부 일정** — 8/3~8/9 중 오늘이면 그날 일정을 대시보드에 띄운다.
- **체크리스트**(사전 준비·입영일·근무 중·퇴영일 17항목) 진행률 + 기기 저장.
- **전체 검색** — 모든 섹션 본문을 훑어 해당 화면으로 이동(`/` 키로 검색창 포커스).

**⚠️ 식사 메뉴는 홍보부 보드와 같은 자료를 본다** (사용자 요청)
- 신규 `functions/api/jp-meals.js` — **읽기 전용**으로 KV `jp:meals` 를 내보낸다. 쓰기는 지금까지처럼 `/api/jamboree-plan` 한 곳뿐이다(두 곳에서 쓰면 서로 덮어쓴다).
- 이 엔드포인트는 **로그인 없이 읽을 수 있다.** `/krjam-fnc` 는 로그인 없는 안내 화면이라 그렇지 않으면 메뉴를 보여 줄 수 없다. 대신 **메뉴 텍스트만** 내보낸다 — `/api/jamboree-plan` 이 로그인 뒤에 있는 이유(연락처·인원 실명)는 여기 담기지 않는다.
- 보드에는 메뉴를 **적어 두지 않는다**(회귀가 감시한다). 적어 두면 홍보부에서 고친 메뉴와 반드시 어긋난다.

**자료 정확도** — 인원·시간·수용인원 등 숫자는 원문에서 그대로 옮겼고, 회귀가 12종을 문자열로 비교한다
(941/157명 · 494+114명 · 299/284명 · 200/50명 · 06:00~07:00 · 16:00~17:00 · 30분간만 …).

**검증**: 신규 `test/regress-krjam-fnc-board.js` **36/36** — 9개 화면이 비지 않는지 · 숫자 12종 · 식권 4종 · 조직도 · 도착 절차 7단계 × 2 · **메뉴가 서버에서 오는지와 보드에 메뉴가 적혀 있지 않은지** · 서버가 죽어도 화면이 사는지 · 카운트다운·식사 상태 계산 · 체크리스트 저장 · 검색 · 원문 딥링크 · 접근성(글자 13px·조작 40px)과 모바일 가로 넘침. 플립북 회귀는 새 경로로 갱신해 **92/92**, 접근성 스윕에 `/krjam-fnc-book` 추가 **36/36**.

**③ 담당 배정** (사용자: "담당자도 배정해줘")
- 구역·업무 **10종**(식자재 보급소 · 1/2/3식당 · 북카페 · CU · 커피차 · 미니 라운지 · 입퇴영 지원 · 상황 보고·안전)에 **담당 · 지원 · 메모**를 둔다.
- 신규 `functions/api/jp-fnc.js` — **읽기는 공개**(로그인 없는 안내 화면이라 그래야 담당자가 보인다), **쓰기는 회원 세션**.
- ⚠️ 로그인 화면을 새로 만들지 않았다. 같은 도메인의 홍보부 보드 세션(`jamboree-plan:session`)이 있으면 그 자리에서 배정할 수 있고, 없으면 읽기 전용 + 안내 문구.
- ⚠️ 업무키는 서버 화이트리스트(`DUTY_KEYS`)로 막는다 — 목록에 없는 키를 받아 주면 화면에 안 보이는 값이 쌓이고, 그게 "저장했는데 안 보인다"가 된다.
- 배정 건수(`배정 N / 10`)와 마지막 수정자·시각을 함께 보여 준다.

**검증 보강**: 보드 회귀 **46/46** — 배정 10종 표시 · 건수·수정자 · **로그인 없으면 못 바꾸는지** · 서버 키와 화면 키가 같은지 · 모르는 키를 버리는지 · 로그인 상태에서 저장이 서버에 도달하는지.

### v0.9.254 — 원문 도표를 화면 안에서 · 정리 · 체크박스 제거 (심호웅 본부장 요청)

**① 원문 도표를 그 자리에서 본다**
- 조직도 · 배치도 · 도착 절차 · 식권 · 일정표처럼 **글로 옮기면 오히려 못 읽는 것**은 원문 그림을 해당 섹션에 함께 놓았다. 누르면 크게 열리고(라이트박스) 좌우로 다음 쪽까지 넘긴다. 목록은 **썸네일 → 원본** 순으로 불러 화면이 먼저 뜬다.
- **도표 모음** 화면 신설 — 자료의 도표 22종을 한자리에.
- ⚠️ 원문 플립북(`/krjam-fnc-book`)은 그대로 살아 있고, 라이트박스에서 "원문에서 열기"로 이어진다.

**② 기능별로 다시 묶음** — 좌측 내비를 `현장에서 바로`(급식·메뉴·편의시설) / `운영`(담당 배정·조직·일정) / `준비`(입퇴영·운영요원) / `자료`(도표·원문)로 나눴다. 모바일 하단 탭도 같은 기준.

**③ 체크박스 제거** (사용자 지시) — 체크리스트 화면을 없애고, 그 내용은 **지급품 · 숙박 · 안전교육 · 근무 기준**으로 나눠 읽는 문서로 정리했다. 목록 표시는 점(·)으로만.

**④ 보기** — 표지 밴드(잼버리 색 그러데이션), 섹션 제목 색 막대, 경고/필수 강조 상자, 2단 배치(글 + 도표).

**검증**: 보드 회귀 **50/50** — 화면 11종 · **체크박스가 없는지** · 도표 모음 20종 이상 · 썸네일 우선 · 라이트박스(원본 쪽·다음 쪽·닫기·원문 링크) · 접근성(13px·40px)과 가로 넘침.

### v0.9.255 — PC 에서 화면을 채운다

- 본문 폭 제한 1180px → **2280px**(홍보부 보드와 같은 기준). 2000px 화면에서 본문 **100%**, 표 **95%** 사용을 실측.
- 넓은 화면(1660px+)에서는 도표 카드 최소폭을 키워 크게 깔린다.
- ⚠️ **긴 문장 문단만 읽기 폭(92~110자)으로 묶었다.** 한 줄이 2000px 를 다 차지하면 눈이 줄을 놓친다 — 표·도표·카드는 화면 폭을 그대로 쓴다.
- 모바일은 손대지 않았다.

**검증**: 보드 회귀 **53/53** — "본문이 화면 폭 90% 이상 · 표가 본문 폭 90% 이상 · 넓은 화면 가로 넘침 0" 3건을 새로 고정(다시 좁아지면 잡힌다).

### v0.9.256 — 디자인 규칙 픽셀 감사 (급식본부 · 홍보부)

`rules/ui-ux.md` 를 화면에서 **직접 재는** 감사 도구를 만들고(`test/audit-krjam-fnc.js` · `test/audit-krjam-planning.js`) 위반을 전부 없앴다.
재는 것 8종 — 글자 13px+ · 대타이틀 20~28px · 조작 요소 40px+ · 음수 자간 -3% 이내 · 카드 중첩 · 시간 HH:MM · 가로 넘침 · **대비(화면 캡처를 픽셀로 디코드해 실측)**. PC 1440·2000 · 모바일 390 × 전 화면.

**급식본부 23건 → 0건** — 상단 대타이틀 17px(규칙 20~28px) → 21px · 좌측 묶음 이름 대비 **2.96:1 → 5.4:1**.

**홍보부 85건 → 0건**
- 🔴 콘텐츠 보드에서 **페이지 전체가 400px 가로로 밀렸다** — 칸반 5열의 최소폭이 부모까지 전파됐다. `#content,.wrap{min-width:0;max-width:100%}` 로 가두어 **스크롤은 보드 안에서만** 일어난다.
- 대타이틀 16px → 21px(모바일 20px) · 검색·날짜 입력 36.8px → 40px.
- 인원·의전 표의 셀 입력 23~38px → **40px**(현장에서 태블릿으로 만지는 화면). 열 배치는 그대로.
- 달력 칸 `+` 가 모바일에서 20px 이고 hover 로만 떠올랐다(터치엔 hover 가 없어 사실상 못 눌렀다) → 터치에서 **항상 보이고 40px**.

두 감사를 회귀 스위트에 넣어 매번 돌린다(한 라운드 13개 검사).

### 19.x v0.9.270 — 급식 관리자 로그인(admin/admin) 신설 [Phase 1/4]
- 사용자: 급식본부 대시보드 관리를 위해 관리자 로그인 추가(id/pw=admin 통일). 목적 = 텍스트/내용 수정·운영요원 개인정보 관리·식사메뉴 편집의 게이트.
- 기존엔 fnc 보드가 홍보부 세션을 재사용했음 → **fnc 전용 관리자 로그인** 신설. 비로그인도 보드 열람은 그대로, 로그인 시 관리자 기능 해제(후속 단계).
- 백엔드 `functions/api/jp-fnc-auth.js`(POST): id/pw=admin/admin 이면 회원세션 인프라로 서명 토큰 발급(payload.fnc=1). 실패도 200 {ok:false}(401 은 콘솔 에러로 찍혀 검증 기준 위반). `_lib.js` 에 `fncAdmin(request,env)` 검증 헬퍼(후속 전화 마스킹·저장 게이트용).
- 클라 `krjam-fnc/board.js`: `fncSession()`(localStorage `krjam-fnc:admin`)·`isAdmin()`(fnc 세션만)·`writeHeader()`(fnc 우선, 없으면 홍보부=메뉴 양방향). 상단바 '관리자' 버튼→로그인 모달(admin/admin), 로그인 시 '관리자 ✓'·로그아웃. Enter/Esc 지원.
- 검증: `node --check`(board·jp-fnc-auth·_lib·test) + fnc-board 회귀 **50→57/57 ×라운드**(초기 비로그인·모달 열림·틀린비번 오류·admin/admin 성공·토큰저장 신규, a11y 스윕[숨은 모달 제외]·콘솔 0·요청실패 0 유지).
- ⏭ 다음: Phase 2 운영요원/쉬프트 캘린더 + 대시보드 실시간 근무자 보드, Phase 3 식사 행열반전+양방향편집, Phase 4 텍스트 인라인 편집.

### 19.x v0.9.271 — 식사 메뉴 행↔열 반전(급식보드 한정) + 양방향 인라인 편집 [Phase 3/4]
- 사용자: 식사 메뉴 가시성 확대(행열 반전, 급식보드 한정) + fnc↔홍보부 양방향 편집.
- fnc `renderMenu` 반전: **행=조식/중식/석식 · 열=날짜(8/3~8/9)**(홍보부 보드는 그대로 행=날짜). `.tblwrap` 가로 스크롤.
- 인라인 편집: fnc 관리자(admin) 또는 홍보부 세션이면 셀 `contenteditable` → blur/Enter 시 `PUT /api/jp-meals {group,date,meal,value}` 로 셀 단위 저장. 같은 jp:meals 라 홍보부에 즉시 반영(양방향).
- 백엔드 `jp-meals.js` 에 **PUT 신설**(GET 전용→편집 가능): 권한 memberOrAdmin ∥ fncAdmin, 셀 단위 read-modify-write(다른 칸 미덮어씀), pickMeals 정규화·400자 제한.
- 검증: `node --check`(board·jp-meals·test) + fnc-board 회귀 **57→60/60 ×3라운드**(표 반전 구조·관리자 셀 편집 21칸·셀 수정→PUT 저장 신규; 기존 '메뉴는 같은 자료' 텍스트 검사 불변).

### 19.x v0.9.272 — 운영요원·쉬프트 캘린더 + 대시보드 실시간 근무자 + 전화 마스킹 [Phase 2/4]
- 사용자: 운영요원 리스트(이름 필수·전화 선택·칩) + 쉬프트(오전05–10·오후11–15·저녁16–21, 8/2~8/9 캘린더) + 대시보드 현재 근무자·오늘 메뉴 실시간 + 전화는 관리자만(평소 이름+끝4자리).
- 백엔드 `functions/api/jp-fnc-staff.js`: KV `jp:fnc-staff`={staff,shifts}. GET 공개(관리자면 전화 전체, 아니면 **phone4**=끝4자리만 — 서버가 전체번호를 비관리자에게 절대 안 보냄). PUT=fncAdmin만. cleanStaff(이름 필수·전화 숫자만)·cleanShifts.
- 프론트 `board.js`: 신규 뷰 **'운영요원 근무'**(명단 칩 + 8/2~8/9×3쉬프트 배정 표). 관리자면 인원 추가(이름/전화)·삭제·쉬프트 배정(select)·해제. 대시보드에 **'지금 근무 · 오늘 식사'** 카드(`renderDutyNow`, 현재 시각→쉬프트 계산, 1초 tick 갱신, 오늘 운영요원 메뉴 표시). `staffLabel`=이름(전화|끝4).
- 검증: `node --check`(board·jp-fnc-staff·test) + fnc-board 회귀 **60→67/67 ×3라운드**(명단 칩·비관리자 마스킹[전체번호 미노출]·배정 UI 게이트·쉬프트 시간계산·관리자 전화전체·추가 저장 PUT 신규; 화면 11→12 카운트 갱신).

### 19.x v0.9.273 — 급식보드 상단바 날씨(Open-Meteo · 강원 고성)
- 사용자: 급식본부도 상단 탭에 날씨 표시. 홍보부 보드와 같은 방식(Open-Meteo 클라 fetch, 백엔드 없음).
- 상단바 `#fnc-wx`: 아이콘·기온·날씨·오늘 최고/최저·강수확률·지역(강원 고성). WMO 코드 매핑, 30분 캐시. 좁은 화면에선 부가정보 단계적 숨김(≤820 날씨문구/최고최저, ≤600 지역/강수).
- 검증: fnc-board 회귀 **67→68/68 ×3라운드**(상단바 기온 표시; open-meteo 는 테스트에서 CORS 헤더 포함 목업으로 결정적 처리 — 콘솔/요청실패 0 유지).

### 19.x v0.9.274 — 텍스트 인라인 편집(Phase 4) + 담당 배정 칩검색 + 편집 게이트 관리자 전용·30분 타임아웃
- 사용자: (1) 담당자 배정은 드롭다운이 아니라 **칩형** — 검색하면 칩 후보가 바로 뜨고 클릭하면 배정. (2) 기능상 겹치면 삭제. (3) **관리자 로그인 시에만 편집 권한**, 로그아웃 시 편집 불가. (4) 로그인 시 "관리자로 로그인되었습니다" 토스트 + **30분 타임아웃**. (5) 담당 배정도 관리자에서만 수정.
- **Phase 4 텍스트 인라인 편집**: 백엔드 `functions/api/jp-fnc-content.js` 신설 — KV `jp:fnc-content`={overrides}. GET 공개, PUT=fncAdmin만, 키 단위 read-modify-write(빈 값=override 제거→기본값 복귀), cleanKey/cleanVal(800자)·최대 300키. 키는 클라가 `'c'+hash(뷰+'|'+원문)` 로 안정 생성(원문 코드 바뀌면 옛 override 자동 폐기). `board.js` `applyContent()` 가 렌더 후 `.card p/h3/h4/li·.sec-h`(링크·컨트롤·동적 영역 CONTENT_SKIP 제외)에 override 적용, 관리자면 `contenteditable`+`.tx-edit`, blur 시 PUT 저장.
- **담당 배정 칩검색**: 쉬프트 배정 UI 를 `<select>` → `<input.shift-search>`+`<div.shift-cands>` 로 교체. 입력하면 `fillShiftCands()` 가 이름 매칭 칩(`.staffchip.cand`) 을 즉시 노출, 클릭 시 `assignShift`.
- **편집 게이트 일원화**: `isAdmin()`=`!!fncSession()`, `canEditMenu()`·`canAssign()` 모두 fnc 관리자 로그인에만 참. 로그아웃 시 모든 편집 UI 사라짐. 로그인 만료(30분, `exp=Date.now()+1800000`) 시 tick() 이 감지해 자동 로그아웃+토스트.
- 검증: `node --check`(board·jp-fnc-content·test) + fnc-board 회귀 **68→73/73 ×3라운드**(비/관리자 tx-edit 게이트·문구 수정→jp-fnc-content PUT·배정 칩검색 후보 노출·후보 클릭→PUT 신규). 회귀 결함 1건 수정: p4 가 심은 `krjam-fnc:admin` 토큰이 **같은 오리진 localStorage 공유**로 p 에 잔류해 비관리자 검사를 오염시켜, p4.close() 직후 p 에서 토큰 제거하도록 고정.

### 19.x v0.9.276~277 — 저장 안정성: 조용한 데이터 유실 경로 차단(동시 저장·전멸·되돌리기)
- 사용자: "시스템 안정성을 검토해줘. 안정적인게 제일 중요해" → "모든 문제들 빨리 다 안정적으로 더블체크하고 다시 수정하고 안정적으로 운영해줘".
- **문제(재현됨)**: 운영요원 명단(`/api/jp-fnc-staff`)과 담당 배정(`/api/jp-fnc`) PUT 은 저장된 값을 **통째로 덮어쓴다**. 두 관리자가 같은 화면을 열어 두고 저장하면 **앞사람 작업이 아무 경고 없이 사라진다**. 화면이 데이터를 못 불러온 상태에서 저장하면 **빈 목록이 그대로 덮인다**.
- **① 동시 저장 차단(낙관적 잠금)**: 불러올 때 받은 `updatedAt` 을 `baseVer` 로 함께 보낸다. 그 사이 다른 사람이 저장했으면 서버가 **409 conflict + 최신본**을 돌려주고, 화면은 덮어쓰지 않고 최신 내용으로 바꾼 뒤 "○○님이 먼저 저장했습니다"라고 알린다. (staff · duty 양쪽)
- **② 전멸 차단(empty_guard)**: 저장된 인원/배정이 있는데 빈 목록이 오면 409 로 막는다. 의도한 초기화는 `confirmEmpty:true` 로만 통과.
- **③ 되돌리기(스냅샷)**: 덮어쓰기 **직전 상태**를 `jp:fnc-staff:snap` 에 최근 10개(30일) 보관. `GET /api/jp-fnc-staff?snapshots=1`(관리자만) 로 목록, `PUT {restore:"<at>"}` 로 복구 — 복구 직전 상태도 다시 스냅샷으로 남긴다. 보호를 다 걸어도 사람은 실수하므로 **되돌릴 길**을 둔다.
- **④ 감사 기록**: 저장·복구를 `appendLog` 로 남긴다(`fnc.staff.save` / `fnc.staff.restore` · 시각·인원수·IP).
- **⑤ 로그인 시도 제한**: `/api/jp-fnc-auth` 는 id/pw 가 단순하다(사용자 지시로 유지). 자동 대량 시도로 세션이 계속 발급되면 편집 권한이 사실상 공개되므로 **IP당 10분 20회** 제한(초과 시 `too_many`). 실패 응답은 200 유지(401 은 콘솔 오류로 찍혀 '콘솔 에러 0' 기준을 깬다).
- **⑥ 불러오기 전 저장 금지 · 실패 시 되돌리기**: 명단·배정 모두 로드 완료 전에는 저장 자체를 하지 않는다. 문구 인라인 편집은 실패 시 `data-orig` 로 원문 복원 — **저장된 척하지 않는다**.
- 검증: `test/regress-fnc-stability.js` 신설 **30/30** — 서버 검사는 **배포되는 모듈 그 자체**를 가짜 KV(Map)로 돌린다(운영 KV 는 읽지도 쓰지도 않음). fnc-board 회귀 73→**75/75**(배정 충돌 시 덮어쓰지 않고 최신본 표시 + 충돌이 저장으로 세지 않음).
- 2026-07-28 사용자 확정: **관리자 id/pw `admin`/`admin` 은 현행 유지** — "급식관리자는 급식본부꺼라 급식본부 본부장님이 별도 요청 전까지는 그대로 유지". 변경은 급식본부 본부장 요청 시에만. 그 대신 대량 시도 제한·세션 30분 만료·저장 잠금/스냅샷/감사기록으로 **되돌릴 수 있게** 받쳐 둔다.

### 19.x v0.9.278 — 담당 배정 안내를 급식 관리자 기준으로 정정(홍보부 문구 제거)
- 사용자 지적: 급식보드인데 담당 배정 안내가 "홍보부 운영보드에 로그인"이라고 안내 — 급식편의본부 편집은 **급식 관리자**가 맞다. (편집 권한 게이트 `canAssign()`=fncSession 은 이미 급식 관리자 전용이었고, **문구만** 홍보부로 남아 있던 것.)
- `board.js` viewDuty lead: "홍보부 운영보드에 로그인" → "**급식 관리자로 로그인**". renderDuty 비관리자 안내: `/krjam-planning` 링크 → `<button data-open-login>`(우측 상단 관리자 로그인 모달을 그 자리에서 연다). `board.css` `.linkbtn`(텍스트형 버튼) 추가.
- ⚠️ 식사 메뉴(menu) 뷰의 홍보부 문구는 **의도된 것**(jp-meals 양방향 공유) — 그대로 둠.
- 검증: fnc-board 회귀 **75→77/77 ×3라운드**(담당 안내에 '급식 관리자로 로그인' 포함·'홍보부'/krjam-planning 링크 없음·로그인 링크 클릭→급식 로그인 모달 열림 신규).

### 19.x v0.9.279 — 사이드바 아이콘 이모지→인라인 SVG(톤 통일) + 메뉴명 정리 + 헤더선 정렬 + 식사 메뉴 문구
- 사용자: (1) 아이콘이 촌스럽다 — 이모지 말고 아이콘 라이브러리에서 가져와 전체 톤 통일. (2) 메뉴 이름을 직관적+스카우트 톤으로. (3) 사이드 헤더선과 상단바 밑선이 틀어져 보인다. (4) 식사 메뉴는 홍보부에도 공유되니 "다시 한 번 확인" 톤으로.
- **아이콘**: 12개 뷰 아이콘을 이모지 → **인라인 SVG**(Lucide/Feather 계열 stroke, `currentColor`·둥근 끝, 상단바 아이콘과 동일 톤)로 교체. `ICONS` 맵 + `svgIcon()` 헬퍼. 활성/hover 시 아이콘 색 변화. 외부 라이브러리·빌드 없음(인라인).
- **메뉴명**: 급식 운영→**급식 현황**, 식사 메뉴→**식단표**, 운영요원 근무→**근무 편성**, 본부·조직→**본부 조직**, 업무 일정→**운영 일정**, 운영요원 안내→**요원 길잡이**, 자료 원문→자료 원문(short 원문). 입영·퇴영/대시보드/편의시설/담당 배정/도표 모음 유지.
- **헤더선 정렬**: `.sidebrand`(64px 고정)와 `.top`(min-height→height 64px 고정, border-box)를 같은 높이로 맞춰 밑선 정렬. `--top-h` 60→64.
- **식사 메뉴 문구**: "홍보부 보드에서 수정" 링크 제거 → "급식본부에서 관리하는 식사 메뉴 · 입력 내용은 홍보부 보드에도 함께 공유되니 저장 전 한 번 더 확인". (부서 구분: [[fnc-vs-hongbo-separate-depts]] — 메뉴 데이터 공유는 유지, 편집 주체는 급식.)
- 검증: fnc-board 회귀 **77→79/79 ×2라운드**(아이콘 인라인 SVG·이모지 잔존 없음·stroke currentColor / 헤더선 정렬 ±1px 신규).

### 19.x v0.9.280 — 조직/인적 단일 저장소(jp-fnc-org) → 담당 배정=조직 로스터 배정표(관리자 편집) [Phase 2a]
- 사용자: 조직도를 표로 만들어 담당 배정에 넣고, **그 인원이 운영요원 근무 등 다른 서브메뉴에도 공유**되게 — "모든 데이터가 한 곳에 올라가 여러 화면에서 운영"되는 게 최우선. 부서/팀 이름도 관리자가 편집 가능하게(오타 정정 포함).
- 백엔드 `functions/api/jp-fnc-org.js` 신설: KV `jp:fnc-org`={org:{head,depts:[{name,chief,teams:[{name,lead,members[]}]}]}}. GET 공개, PUT fncAdmin. **낙관적 잠금**(baseVer→409 conflict)·**빈 조직 가드**(confirmEmpty)·`cleanOrg`(빈 부서/팀명 버림·공백 접기·팀원 정리·개수 상한). 연락처 미포함(이름만).
- 프론트 `board.js`: **담당 배정 = 조직 로스터 표**로 교체(기존 구역·업무 담당/지원 placeholder 배정표 제거). 부서·부장 / 팀·팀장 / 팀원 3열, 부서 rowspan. 관리자면 '조직 편집' → 부서명·부장·팀명·팀장 input + 팀원 textarea(쉼표/줄바꿈)로 통째 편집 후 PUT. 저장 성공 시 운영요원 근무도 갱신(`renderStaffView`). ORG 는 서버값이 있으면 그것으로, 없으면 내장 시드. `peopleList()`/`getOrg()` 노출.
- ⚠️ 기존 담당 area-assignment(`jp-fnc.js` duties)·`__fncBoard.DUTIES` 제거. `jp-fnc.js` API 파일과 회귀의 옛 목업은 사용 안 하지만 무해하게 남겨둠(다음에 정리).
- 검증: `node --check`(board·jp-fnc-org·test) + fnc-board 회귀 **79/79 ×N라운드**(조직표 부서4·팀5 시드·부장/팀장/팀원 표시·인원수·비관리자 편집 불가·급식 관리자 안내·cleanOrg 정리·팀장/부서명 편집 저장·낙관적 잠금 충돌 최신본 신규).
- 🔜 Phase 2b: 운영요원 근무 쉬프트 후보를 조직 로스터에서 직접(현재는 별도 staff 목록).

### 19.x v0.9.281 — 운영요원 근무 인원 = 조직 로스터(단일 원본) · 전화는 근무화면 별도 [Phase 2b]
- 사용자: "모든 인원이 운영요원 근무에도 보이게" — 이름은 조직(담당 배정)에서, 전화는 근무 화면에서 별도 관리(사용자 확정).
- `board.js`: 운영요원 근무의 **근무 후보/명단 = 조직 로스터**(`rosterNames()`=peopleList, 충원예정 제외). 쉬프트 배정을 **id→이름** 키로 전환(assign/unassign/후보검색 모두 이름). 대시보드 '지금 근무'도 이름 기반. 수동 인원 추가/삭제 UI 제거(인원 원본은 조직표). 전화는 이름별 입력(`data-phone`) + '연락처 저장'(`savePhones`) — `jp-fnc-staff` 를 **이름→전화 레지스트리**로 재사용(비관리자 phone4 유지). `staffLabel(name)`·`phoneMap()` 신설.
- `saveStaff` 는 `confirmEmpty:true`(staff 가 이제 선택적 전화 레지스트리 — 빈 값도 정상. 낙관적 잠금·스냅샷으로 보호). 서버 `jp-fnc-staff.js` 스키마 그대로(문자열 배열=이름) — 무변경.
- `board.css`: `.staffrow`/`.phone-in`/`.staff-roster.editing` 그리드.
- ⚠️ 마이그레이션: 라이브 `jp:fnc-staff` 는 비어 있어(이전 수동입력 없음) id→이름 전환 영향 없음. 기존 데이터가 있었다면 옛 id 배정은 이름과 매칭 안 돼 사라질 수 있음(해당 없음).
- 검증: `node --check`(board·test) + fnc-board 회귀 **80/80 ×N라운드**(명단=조직 로스터·비관리자 phone4·관리자 이름별 전화 입력/저장/재표시·이름 기반 배정 칩검색 신규).

### 19.x v0.9.283 — 운영요원 식사메뉴 확정본 반영 + fnc 관리자 계정 변경
- 사용자: (1) 급식 메뉴 최종 PDF(2026-07-28) 반영. (2) fnc 관리자 계정 → **foodservice / 20260803**.
- **메뉴(staff)**: `jamboree-plan/app.js` `defaultMeals().staff` 를 확정 PDF 기준으로 전면 재작성. 기존 seed 가 **조식이 하루 밀려 있고**(8/3 조식 존재 — 입영 14:00이라 조식 없음이 맞음) 오타(뽀로로두부북→봉·오이고추짱장→쌈장·부추걸절이→겉절이·얼큰 무채어묵탕→얼큰계란감자국)가 있어 교정. 음료/디저트는 `[ ]` 로, 조식 고정메뉴·제철과일은 MEAL_NOTE 유지. 1회성 멱등 마이그레이션 `migrateMealsStaff()`(가드: 8/8 조식 '돈육짜장덮밥') 추가 — crew_n/crew_s 불변. 메뉴는 공유 데이터라 홍보부 보드에도 반영(fnc↔홍보부 동일 jp:meals).
- **계정**: `functions/api/jp-fnc-auth.js` 인증을 admin/admin → **foodservice/20260803**. 관련 주석·회귀 로그인 케이스 갱신.
- 검증: regress-krjam-planning **149/149**(운영요원 확정본 값·마이그레이션 멱등 신규) + fnc-board **80/80**(foodservice 로그인) ×2라운드.

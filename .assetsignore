# Files that live in the repo but must NOT be served publicly on the web.
# (Cloudflare Pages serves everything in the output dir unless ignored here.)
# Keep app assets (*.html, *.js, *.css, data.js, VERSION, _headers, functions/) served.

# Internal documentation (contains admin paths, env var names, passwords, KV ids)
*.md

# Build / deploy config
wrangler.toml
package.json
package-lock.json
.gitignore
.assetsignore

# Legacy GitHub Pages domain file (leaks the domain; CF uses dashboard custom domains)
CNAME

# Local tooling
.claude
node_modules
